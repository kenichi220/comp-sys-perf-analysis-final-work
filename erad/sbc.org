# -*- coding: utf-8 -*-
# -*- mode: org -*-

#+TITLE: Performance Evaluation of Tensorflow Parallel Strategies for Deep Learning Training on HPC Clusters
#+AUTHOR: Rayan Raddatz de Matos

#+STARTUP: overview indent
#+LANGUAGE: en
#+OPTIONS: H:3 creator:nil timestamp:nil skip:nil toc:nil num:t ^:nil ~:~
#+OPTIONS: author:nil title:nil date:nil
#+TAGS: noexport(n) deprecated(d) ignore(i)
#+EXPORT_SELECT_TAGS: export
#+EXPORT_EXCLUDE_TAGS: noexport

#+LATEX_CLASS: article
#+LATEX_CLASS_OPTIONS: [12pt]

#+LATEX_HEADER: \sloppy

# PDF generation can be done by make (thanks Luka Stanisic)
#   or C-c C-e l p (thanks Vinicius Garcia)

* *The Paper*                                                       :ignore:
** Frontpage                                                        :ignore:

#+BEGIN_EXPORT latex
\makeatletter
\let\orgtitle\@title
\makeatother

\title{\orgtitle}

\author{
   Rayan Raddatz de Matos\inst{1},
   Kenichi Brumato\inst{1},
   Marcelo Cardoso Oliveira Gulart\inst{1}
}

\address{
   Institute of Informatics, Federal University of Rio Grande do Sul (UFRGS)\\
   Caixa Postal 15.064 -- 91.501-970 -- Porto Alegre -- RS -- Brazil
   \email{\{rayan.raddatz, kbrumati, mcogulart\}@inf.ufrgs.br}}
#+END_EXPORT

#+LaTeX: \maketitle


** Abstract                                                         :ignore:

#+begin_abstract
The growing complexity of Deep Learning models makes training on single devices difficult, requiring distributed strategies like Data Parallelism. However, this approach creates critical synchronization challenges between processing units. This work evaluates TensorFlow parallel strategies on an HPC cluster to analyze how application factors contribute to time reduction. The study compares the standard synchronous method against a proposed Custom Training Loop designed to reduce bottleneck by synchronizing gradients less frequently. Experiments using ResNet50 and EfficientNetB0 models show that the Custom Training Loop significantly lowers communication overhead compared to the standard approach. Additionally, results indicate that increasing the batch size consistently improves performance. The authors conclude that correct configuration allows for performance gains in distributed training, even on networks with lower throughput.
#+end_abstract

** Introduction

In the last decade, Deep Learning has established itself as the
state-of-the-art for solving complex problems, such as computer vision
and natural language processing. Given the progress in algorithmic
precision, there has been an exponential growth in the complexity of
neural network architectures and the volume of datasets. This scenario
imposes a massive computational load, making the training of robust
models on a single processing unit an difficult task in terms of time
and resources.

To mitigate these challenges, distributed training has become a
standard practice, with an emphasis on the Data Parallelism
strategy. In this approach, a complete replica of the model is
instantiated on each processing unit, and the global
dataset is partitioned into smaller, locally-focused batches. Although
effective, this technique introduces critical synchronization
challenges, as locally calculated gradients must be aggregated and
communicated among all devices at every training step, typically
through All-Reduce operations.

Given this fact, in this work we decided to analyze data parallelism
for AI training using TensorFlow in a HPC cluster to investigate how
the factors related to the application contribute to time reduction in
a parallel scenario.

#+latex: \noindent
*Software and Data Availability*. We endeavor to make our analysis
reproducible for a better science. We made available a companion
material hosted in a public GitHub repository at
#+latex: {\scriptsize\url{https://github.com/kenichi220/comp-sys-perf-analysis-final-work/tree/main}}.
Our companion contains the source code of this article and the
software necessary to handle the created datasets. We also include
instructions to run the experiment and figures.
# An archive is also available in Zenodo at URL.

** Related Work

[This should be a small section]
[Marcelo will write about related works]

** COMMENT Background and Experimental Context
\label{sec.context}
*** Background
\label{sec.background}

TensorFlow is a machine learning system that operates at a large scale
and in heterogeneous \cite{tensorflow2016}. It uses dataflow graphs to
represent the computation, shared state, and the operations that alter
this state.

This architecture maps the nodes of a dataflow graph onto many
machines within a cluster and, within a machine, onto multiple
computational devices (multicore CPUs, GPUs, and TPUs). This offers
flexibility to the developer to experiment with new optimizations and
training algorithms, supporting a variety of applications focused on
deep neural networks. When it comes to distributed training, TensorFlow offers different
strategies with the =tf.distribute.Strategy= API. The TensorFlow
documentations adverts that /"For synchronous training on many GPUs
onmultiple workers, use the tf.distribute.MultiWorkerMirroredStrategy with the Keras Model.fit or a custom training loop."/\cite{distributed}. Meaning for a
distributed training across different nodes in a cluster there is two
options: Using the MultiWorkerMirroredStrategy with the model.fit or
creating a Custom Training Loop by yourself.

*** Experimental Context
In High-Performance Computing (HPC) environments, a highly recommended
practice to access to shared computational resources is to mediate the
access by workload management systems. In our context Slurm (Simple
Linux Utility for Resource Management) is utilized. Slurm is an
open-source, fault-tolerant, and highly scalable job scheduler,
responsible for three main functions: Allocating exclusive or
non-exclusive access to resources/nodes for users, providing a
framework to initiate, execute, and monitor the work on the set of
allocated nodes and managing a queue of pending jobs \cite{yoo2003slurm}.

Interaction with Slurm is typically performed through shell scripts
written in the BASH language. These scripts contain preprocessing
directives that specify resource requirements, such as the number of
nodes, tasks per node, and execution time limit, enabling the
automation and reproducibility of computational experiments.


** COMMENT Related Work and Motivation
\label{sec.relatedwork}

** Methods and Materials

*** Software Management and Reproducibility

Reproducibility of computational experiments is a critical challenge
in computer science. To mitigate software dependency issues, the use
of the GNU Guix package manager was explored. Guix implements the
functional package management paradigm where each package is
installed in a unique and immutable directory in the store, derived
from a cryptographic hash of its dependencies \cite{courtes2013guix}.

However, the efficient training of deep neural networks using
TensorFlow requires the use of proprietary NVIDIA hardware
acceleration libraries, which are not officially distributed in
standard Guix channels due to its strict Free Software policy.

Given this restriction, the strategy adopted in this work is the use
of Python Virtual Environment for environment management. Although a
functional version of Guix exists on GitHub without the proprietary
libraries.

For managing the specific Deep Learning libraries, the python package
manager =pip= with version 25.3 was utilized. The exact version of the used library was
frozen and documented using the =pip freeze command=, generating a
requirements file (=reqs.txt=) that enables the reconstruction of the
execution environment used in the experiments.

*** Hardware and Software Configuration

The experiments were run using TensorFlow 2.15 as newer versions
showed errors when trying to train using more than one node with the
=.fit= and =MultiWorkerMirrodStrategy= and python 3.11.2. The NVIDIA
drivers version was 550.54.15 and CUDA 12.4. We used up to three
computational nodes connected with a network of 1G, each node contains an
Intel(R) Core(TM) i7-14700KF processor at 3.40 GHz with 28 threads and
20 cores, 96 GB DDR5 RAM and NVIDIA GeForce RTX 4070 with 8GB memory
accelerator.

*** Custom Training Loop

The expected execution from training with the
=MultiWorkerMirroredStrategy= would show a slower training time as we
increased the number of nodes in the execution. However, we faced a
problem due to the connection while training that resulted in a higher
training time as we increased the number of nodes for big models with
a lot of parameters such as ResNet50. This problem happened because a
synchronization is done for every batch computed by the =model.fit()=
training method, and as we had only an 1G network, this
synchronization resulted in a bottleneck that increased the training
time. To mitigate this problem, we proposed a Custom Training Loop
that instead of synchronizing the parameters between nodes in every
batch, only synchronizes at a interval of batches, then resulting in a
lower synchronization overhead. To this work we defined this
interval of batches as 4 batches.

*** Design of Experiments (DoE)

We selected two different Deep Learning models to train: *ResNet50*
\cite{resnet} as a big model with ~25.6M parameters and
*EfficientNetB0*\cite{efficientNet} as a small model with ~5.3M
parameters. The two networks were trained using the TinyImageNet-200,
a subset of the ImageNet\cite{imagenet} containing 100000 downsized to
a 64×64 colored image divided among 200 classes. We created a full
factorial for each model with varying batch size, the number of nodes
used and the type of the training. The levels for the batch size
factor were 32, 64 and 100, for the nodes we trained with 1, 2 and 3
nodes and for the type of training we had "sync" that uses the
=model.fit= method to train or "ctl" that uses our custom training loop
to train. This is a total of 2x3x3x2 different executions, each
execution as replicated 5 times in a random order. Our output variable
was the training time (full training time and time per epoch).


** COMMENT Challenges

Multi-node communication faced a bottleneck as the node \texttt{poti4}
was connected with a limited bandwidth of 100 Mbps. Regarding software
management, GNU Guix does not natively support proprietary libraries
due to its strict policy requiring free and open-source software.

** Loading data                                            :noexport:ignore:

*** Load combinations and run ID
#+begin_src R :results output :session *R* :exports both
options(crayon.enabled=FALSE)
suppressMessages(library(fs))
suppressMessages(library(tidyverse))
suppressMessages(library(janitor))
suppressMessages(library(dplyr))
suppressMessages(library(lubridate))

BASE="data/final/"
tibble(PATH = dir_ls(BASE, regexp = "out$", recurse=TRUE))  |> print() |>
  ## filter(!str_detect(CSV, ":")) |>
  ## filter(!str_detect(CSV, "ep")) |>
  ## mutate(DATA = map(CSV, read_csv, progress=FALSE, show_col_types = FALSE)) |>
  separate(PATH, into=paste0("XX", 1:3), sep="/") |>
  separate(XX3, into=paste0("YY", 1:6), sep="_") |>
  ## mutate(type=XX4) |> #|>
  mutate(YY6 = gsub("\\.out", "", YY6)) |>
  ## mutate(machine = gsub("\\-.*", "", machine)) |>
  ## rowid_to_column("id") |>
  select(id=YY6, model=YY2, batch=YY4, type=YY3, nodes=YY5) -> configs

configs
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 230 × 1
   PATH
   <fs::path>
 1 data/final/train_EfficientNetB0_ctl_100_1_735831.out
 2 data/final/train_EfficientNetB0_ctl_100_1_735908.out
 3 data/final/train_EfficientNetB0_ctl_100_1_735920.out
 4 data/final/train_EfficientNetB0_ctl_100_1_735970.out
 5 data/final/train_EfficientNetB0_ctl_100_1_736028.out
 6 data/final/train_EfficientNetB0_ctl_100_1_736718.out
 7 data/final/train_EfficientNetB0_ctl_100_1_736719.out
 8 data/final/train_EfficientNetB0_ctl_100_1_736720.out
 9 data/final/train_EfficientNetB0_ctl_100_1_736721.out
10 data/final/train_EfficientNetB0_ctl_100_1_736727.out
# ℹ 220 more rows
# ℹ Use `print(n = ...)` to see more rows
# A tibble: 230 × 5
   id     model          batch type  nodes
   <chr>  <chr>          <chr> <chr> <chr>
 1 735831 EfficientNetB0 100   ctl   1
 2 735908 EfficientNetB0 100   ctl   1
 3 735920 EfficientNetB0 100   ctl   1
 4 735970 EfficientNetB0 100   ctl   1
 5 736028 EfficientNetB0 100   ctl   1
 6 736718 EfficientNetB0 100   ctl   1
 7 736719 EfficientNetB0 100   ctl   1
 8 736720 EfficientNetB0 100   ctl   1
 9 736721 EfficientNetB0 100   ctl   1
10 736727 EfficientNetB0 100   ctl   1
# ℹ 220 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

*** Get the data and join with the run
#+begin_src R :results output :session *R* :exports both
BASE="data/final/logs/"
tibble(CSV = dir_ls(BASE, regexp = "csv$", recurse=TRUE)) |>
  ## filter(!str_detect(CSV, ":")) |>
  ## filter(!str_detect(CSV, "ep")) |>
  mutate(DATA = map(CSV, read_csv, progress=FALSE, show_col_types = FALSE)) |>
  separate(CSV, into=paste0("XX", 1:7), sep="/") |>
  separate(XX4, into=paste0("YY", 1:2), sep="_")  |>
  select(id=YY2, DATA) |>
  mutate(
    DATA = map(DATA, function(x) {
      if (is.data.frame(x)) {
        return(x %>% mutate(epoca = as.character(epoca),
                            tempo = as.character(tempo)))
      } else {
        return(x)
      }
    })
  ) |>
  unnest(DATA) -> results

results
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 804 × 3
   id     epoca tempo
   <chr>  <chr> <chr>
 1 735818 0     298.360699892044
 2 735818 1     275.497747659683
 3 735818 2     274.337224721909
 4 735818 3     274.618661165237
 5 735818 4     273.248296737671
 6 735824 0     337.033883571625
 7 735824 1     317.953343868256
 8 735824 2     318.067031621933
 9 735824 3     317.879886865616
10 735824 4     317.93927025795
# ℹ 794 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

*** Join the data

#+begin_src R :results output :session *R* :exports both
df <- configs |>
  left_join(results, by="id")

df
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 837 × 7
   id     model          batch type  nodes epoca tempo
   <chr>  <chr>          <chr> <chr> <chr> <chr> <chr>
 1 735831 EfficientNetB0 100   ctl   1     <NA>  <NA>
 2 735908 EfficientNetB0 100   ctl   1     0     350.095215320587
 3 735920 EfficientNetB0 100   ctl   1     0     344.294260263443
 4 735920 EfficientNetB0 100   ctl   1     1     333.430016517639
 5 735970 EfficientNetB0 100   ctl   1     0     344.588366985321
 6 736028 EfficientNetB0 100   ctl   1     <NA>  <NA>
 7 736718 EfficientNetB0 100   ctl   1     0     344.885010719299
 8 736719 EfficientNetB0 100   ctl   1     0     345.933742523193
 9 736719 EfficientNetB0 100   ctl   1     1     334.544350147247
10 736720 EfficientNetB0 100   ctl   1     0     344.251515865326
# ℹ 827 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

*** Runs with error
#+begin_src R :results output :session *R* :exports both
df |> group_by(nodes, type, model, batch, id) |>
  summarize(N=n(), .groups="keep") |>
  arrange(N) |>
  filter(N < 5 | N > 5) |> print() |>
  select(-N) -> errors

errors |> print(n=100)

do.call("rbind", replicate(5, errors, simplify = FALSE)) |>
  write_csv("errors_rerun.csv", progress=FALSE)
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 62 × 6
# Groups:   nodes, type, model, batch, id [62]
   nodes type  model          batch id         N
   <chr> <chr> <chr>          <chr> <chr>  <int>
 1 1     ctl   EfficientNetB0 100   735831     1
 2 1     ctl   EfficientNetB0 100   735908     1
 3 1     ctl   EfficientNetB0 100   735970     1
 4 1     ctl   EfficientNetB0 100   736028     1
 5 1     ctl   EfficientNetB0 100   736718     1
 6 1     ctl   EfficientNetB0 100   736721     1
 7 1     ctl   EfficientNetB0 100   736727     1
 8 1     ctl   EfficientNetB0 100   736728     1
 9 1     ctl   EfficientNetB0 100   736729     1
10 1     ctl   EfficientNetB0 100   736730     1
# ℹ 52 more rows
# ℹ Use `print(n = ...)` to see more rows
# A tibble: 62 × 5
# Groups:   nodes, type, model, batch, id [62]
   nodes type  model          batch id
   <chr> <chr> <chr>          <chr> <chr>
 1 1     ctl   EfficientNetB0 100   735831
 2 1     ctl   EfficientNetB0 100   735908
 3 1     ctl   EfficientNetB0 100   735970
 4 1     ctl   EfficientNetB0 100   736028
 5 1     ctl   EfficientNetB0 100   736718
 6 1     ctl   EfficientNetB0 100   736721
 7 1     ctl   EfficientNetB0 100   736727
 8 1     ctl   EfficientNetB0 100   736728
 9 1     ctl   EfficientNetB0 100   736729
10 1     ctl   EfficientNetB0 100   736730
11 1     ctl   EfficientNetB0 100   736731
12 1     ctl   EfficientNetB0 100   736737
13 1     ctl   EfficientNetB0 100   736738
14 1     ctl   EfficientNetB0 100   736739
15 1     ctl   EfficientNetB0 100   736740
16 1     ctl   EfficientNetB0 100   736741
17 1     ctl   EfficientNetB0 100   736747
18 1     ctl   EfficientNetB0 100   736748
19 1     ctl   EfficientNetB0 100   736749
20 1     ctl   EfficientNetB0 100   736751
21 1     ctl   EfficientNetB0 100   736757
22 1     ctl   EfficientNetB0 100   736760
23 1     ctl   EfficientNetB0 100   736767
24 2     ctl   EfficientNetB0 100   735857
25 2     ctl   EfficientNetB0 100   735864
26 2     ctl   EfficientNetB0 100   735916
27 2     ctl   EfficientNetB0 100   735969
28 2     ctl   EfficientNetB0 100   736013
29 2     ctl   EfficientNetB0 100   736722
30 2     ctl   EfficientNetB0 100   736723
31 2     ctl   EfficientNetB0 100   736724
32 2     ctl   EfficientNetB0 100   736725
33 2     ctl   EfficientNetB0 100   736726
34 2     ctl   EfficientNetB0 100   736732
35 2     ctl   EfficientNetB0 100   736734
36 2     ctl   EfficientNetB0 100   736735
37 2     ctl   EfficientNetB0 100   736736
38 2     ctl   EfficientNetB0 100   736742
39 2     ctl   EfficientNetB0 100   736743
40 2     ctl   EfficientNetB0 100   736745
41 2     ctl   EfficientNetB0 100   736746
42 2     ctl   EfficientNetB0 100   736752
43 2     ctl   EfficientNetB0 100   736753
44 2     ctl   EfficientNetB0 100   736754
45 2     ctl   EfficientNetB0 100   736755
46 2     ctl   EfficientNetB0 100   736762
47 2     ctl   EfficientNetB0 100   736763
48 2     ctl   EfficientNetB0 100   736764
49 2     ctl   EfficientNetB0 100   736766
50 3     ctl   EfficientNetB0 100   735847
51 3     ctl   EfficientNetB0 100   735865
52 1     ctl   EfficientNetB0 100   735920
53 1     ctl   EfficientNetB0 100   736719
54 1     ctl   EfficientNetB0 100   736720
55 1     ctl   EfficientNetB0 100   736750
56 1     ctl   EfficientNetB0 100   736761
57 2     ctl   EfficientNetB0 100   736733
58 2     ctl   EfficientNetB0 100   736744
59 1     ctl   EfficientNetB0 100   736758
60 1     ctl   EfficientNetB0 100   736759
61 2     ctl   EfficientNetB0 100   736756
62 2     ctl   EfficientNetB0 100   736765
#+end_example

*** Remove runs with error
#+begin_src R :results output :session *R* :exports both
df.clean <- df |> filter(!is.na(epoca)) |>
  group_by(nodes, type, model, batch, id) |>
  filter(n() == 5) |>
  ungroup()


df.clean
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 760 × 7
   id     model          batch type  nodes epoca tempo
   <chr>  <chr>          <chr> <chr> <chr> <chr> <chr>
 1 735858 EfficientNetB0 32    ctl   1     0     333.319829463959
 2 735858 EfficientNetB0 32    ctl   1     1     312.819135427475
 3 735858 EfficientNetB0 32    ctl   1     2     319.11979341507
 4 735858 EfficientNetB0 32    ctl   1     3     319.29111123085
 5 735858 EfficientNetB0 32    ctl   1     4     314.691659450531
 6 735895 EfficientNetB0 32    ctl   1     0     326.055331468582
 7 735895 EfficientNetB0 32    ctl   1     1     312.437191724777
 8 735895 EfficientNetB0 32    ctl   1     2     312.249012231827
 9 735895 EfficientNetB0 32    ctl   1     3     313.085581064224
10 735895 EfficientNetB0 32    ctl   1     4     312.25504565239
# ℹ 750 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

*** Graphs style
#+begin_src R :results output :session *R* :exports both

meu_estilo <- function() {
  list(
    theme_bw(base_size = 20),
    theme(
      legend.title = element_blank(),
      plot.margin = unit(c(0, 0, 0, 0), "cm"),
      legend.spacing = unit(1, "mm"),
      legend.position = "top",
      legend.justification = "left",
      legend.box.spacing = unit(0, "pt"),
      legend.box.margin = margin(0, 0, 0, 0),
      axis.text.x = element_text(angle=45, vjust=1, hjust=1)
    ))
}
#+end_src

#+RESULTS:



#+RESULTS:
: Error: object 'data_treino' not found

*** Plot results

Filtering the data for the graphs
#+begin_src R :results output :session *R* :exports none
df.clean |> filter(!is.na(epoca)) |> #group_by(model, batch, type, nodes, epoca_) |>

  mutate(
    # 'epoca' must be numeric for plotting
    epoca = as.numeric(epoca),
    # 'tempo' must be numeric for plotting
    tempo = as.numeric(tempo),
    # Create a unique identifier for each configuration/run (e.g., combining ID and Configs)
    # Replace 'config_column_1' and 'config_column_2' with your actual column names.
    config_run = paste(model, batch, type, nodes, sep = "_")
  ) |>
  group_by(id) |>
  mutate(tempo_treino = sum(tempo)) |>
  ungroup() -> data_plot

data_plot
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 760 × 9
   id     model          batch type  nodes epoca tempo config_run              tempo_treino
   <chr>  <chr>          <chr> <chr> <chr> <dbl> <dbl> <chr>                          <dbl>
 1 735858 EfficientNetB0 32    ctl   1         0  333. EfficientNetB0_32_ctl_1        1599.
 2 735858 EfficientNetB0 32    ctl   1         1  313. EfficientNetB0_32_ctl_1        1599.
 3 735858 EfficientNetB0 32    ctl   1         2  319. EfficientNetB0_32_ctl_1        1599.
 4 735858 EfficientNetB0 32    ctl   1         3  319. EfficientNetB0_32_ctl_1        1599.
 5 735858 EfficientNetB0 32    ctl   1         4  315. EfficientNetB0_32_ctl_1        1599.
 6 735895 EfficientNetB0 32    ctl   1         0  326. EfficientNetB0_32_ctl_1        1576.
 7 735895 EfficientNetB0 32    ctl   1         1  312. EfficientNetB0_32_ctl_1        1576.
 8 735895 EfficientNetB0 32    ctl   1         2  312. EfficientNetB0_32_ctl_1        1576.
 9 735895 EfficientNetB0 32    ctl   1         3  313. EfficientNetB0_32_ctl_1        1576.
10 735895 EfficientNetB0 32    ctl   1         4  312. EfficientNetB0_32_ctl_1        1576.
# ℹ 750 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

da criação de um modelo linear
*** COMMENT See how the time was by epoch

#+begin_src R :results output file graphics :file (org-babel-temp-file "figure" ".pdf") :exports results :width 12 :height 8 :session *R*

data_plot |> #filter(grepl("*B0*", model)) |>
  group_by(config_run, epoca) |>
  mutate(
    N = n(),
    mean_tempo = mean(tempo, na.rm = TRUE),
    sd_tempo = sd(tempo),
    se_tempo = 3 * sd(tempo) / sqrt(N),
  ) |>
  mutate(
    lower_bound = mean_tempo - se_tempo,
    upper_bound = mean_tempo + se_tempo
  ) |>
  ggplot(aes(x = epoca, y = mean_tempo, color = as.factor(epoca), shape=model)) +
  geom_jitter(
    data = data_plot,
    aes(x = epoca, y = tempo, color = as.factor(epoca), group = config_run),
    # Set the transparency (alpha) to 0.3 for a faint look
    alpha = 0.3,
    size = 2
  ) +
  geom_line(linewidth = 1, alpha = 0) +
  # Add points for the mean of the epochs
  geom_point(size = 3, alpha = 0) +
  geom_errorbar(
    aes(ymin = lower_bound, ymax = upper_bound),
    width = 0.2, # Controls the width of the horizontal bar at the end of the error line
    linewidth = 0.6, alpha = 0
  ) +
  facet_grid(batch~type ~ nodes) +
  meu_estilo()
#+end_src

#+RESULTS:
[[file:/tmp/babel-6twXnP/figureGh7TWu.pdf]]

*** COMMENT See time by training

#+begin_src R :results output graphics file :file (org-babel-temp-file "figure" ".pdf") :exports results :width 6 :height 4 :session *R*
data_plot |> #filter(grepl("*B0*", model)) |>
  group_by(id, type, model, batch, nodes) |>
  summarize(
    tempo_treino = sum(tempo, na.rm = TRUE),
    ) |>
  ungroup() |>
  mutate(batch = as.integer(batch)) |>
  print() |>
  ##   mutate(
  ##   lower_bound = mean_tempo - se_tempo,
  ##   upper_bound = mean_tempo + se_tempo
  ## ) |>

  ggplot(aes(x = as.factor(nodes), y = tempo_treino, color=model, shape=nodes)) +

  geom_jitter(    # Set the transparency (alpha) to 0.3 for a faint look
    alpha = 0.8,
    size = 2
  ) +
  geom_line(linewidth = 1, alpha = 0) +

    facet_grid(type ~ batch) +
  labs(
    y = "Training Time [Seconds]",
    x = "Nodes"
  ) +

meu_estilo()
#+end_src

#+RESULTS:
[[file:/tmp/babel-syZuVj/figureDHaYfk.png]]


** Results

Figure 1 show a full view of our results. The graph presents the mean
training time in minutes as a function of the number of nodes, faceted
by type and batch size and with the color representing the model. Each
bar represents the mean for the five executions for each
configuration. The black transparent dots represents our
observations. This allow us to see two main results: The first one is
that the inclusion of more then one node introduces the necessity of
communication, and together communication overhead. This overhead can
be better seen with the smaller batches, as the computation is faster
and more communication is needed. We can also see that the ResNet50
shows a higher time then the EfficientNetB0 in this cases. This
happens because all the parameters needed to be synchronized after
every batch, and the ResNet50 is almost 5 times bigger than the
EfficientNetB0. The second one is that the ctl showed a considerable
smaller communication overhead even for small batch sizes. The ctl
showed *OOM* errors when running with the batch size 100 for training
the EfficientNetB0 model.

After the communication overhead introduce by the inclusion of a
second node, adding more nodes seemed to decrease the training
time. We suppose that with enough nodes, the time gained by computing
can overcome the overhead and be better then running it in one machine.

#+begin_src R :results output file graphics  :file (org-babel-temp-file "figure" ".pdf") :exports results :width 8 :height 4 :session *R*

data_plot |> #filter(grepl("*B0*", model)) |>
  group_by(id, type, model, batch, nodes) |>
  mutate(
    tempo_treino = sum(tempo, na.rm = TRUE) / 60,
  ) |>
  ungroup() |>
  mutate(batch = as.integer(batch)) -> data_treino

data_treino |> group_by(id, type, model, batch, nodes) |>
  summarize(
    tempo_treino = sum(tempo)/60
  ) |> ungroup() -> data_points


data_treino |>
  group_by(type, model, batch, nodes) |>
  summarize(
    N = n(),
    mean_treino = mean(tempo_treino, na.rm = TRUE),
    sd_treino = sd(tempo_treino),
    se_treino = 3 * sd(tempo_treino) / sqrt(N),
  ) |>
  ungroup() |>
  mutate(
    lower_bound = mean_treino - se_treino,
    upper_bound = mean_treino + se_treino
  ) -> data_treino.plot

data_treino.plot |>
  ggplot(aes(x = as.factor(nodes), y = mean_treino,
             color=model,
             fill=model)) +


  ## geom_line(aes(y = mean_treino, group = model), # <--- Mapeie a linha para a média
  ##         linewidth = 0.3
  ##       , alpha = 1) +

  geom_col(  # Set the transparency (alpha) to 0.3 for a faint look
    position = position_dodge(width = 0.9)
  ) +


  geom_jitter(
    data = data_points,
    aes(x = as.factor(nodes), y = tempo_treino), # Mapeamento X/Y do dado bruto
    # Adicione color=model NO AES() para que o dodge funcione
    # Você já tem fill=model e color=model no mapeamento global.
    # Se você quiser uma cor neutra para os pontos:
    color = "gray30", # Cor fixa fora do aes()
    alpha = 0.3,
    size = 1.5,
    # Use a mesma posição de desvio EXATA
    position = position_jitterdodge(dodge.width = 0.9,
        jitter.width = 0.35)
  ) +
  ## geom_line(linewidth = 1, alpha = 0) +


  geom_errorbar(aes( ymin = lower_bound, ymax = upper_bound),
                width=.8, alpha=.7,
                position = position_dodge(width = 0.9),
                color="black") +

  facet_grid(batch ~ type) +
  labs(
    y = "Training Time [Minutes]",
    x = "Nodes"
  ) +
  scale_fill_manual(values = c("firebrick2", "#2D72AD")) +
  scale_color_manual(values = c("firebrick2", "#2D72AD")) +

  meu_estilo()
#+end_src


#+CAPTION: Mean of experimental results.
#+NAME:fig:results
#+attr_latex: :placement [!htb] :width \linewidth
#+RESULTS:
[[file:/tmp/babel-IGtNc7/figureTZRjku.pdf]]


*** The impact of node number and batch size

Figure 2 shows the mean training time grouped by node count. This
enable us to see that in fact the increase of more then two machines
results in a lower training time even though this gain is
small. Figure 3 shows also an interesting result, we can see that
increase of the batch size consistently show performance gains, but
also decrease the variance. This means that conforms the batch size
grows the training time is almost the same despite the node count.

#+begin_src R :results output file graphics  :file (org-babel-temp-file "figure" ".pdf") :exports results :width 8 :height 4 :session *R*

data_plot |> #filter(grepl("*50*", model)) |>
  group_by(id, type, model, nodes) |>
  mutate(
    tempo_treino = sum(tempo, na.rm = TRUE) / 60,
  ) |>
  ungroup() |>
  mutate(batch = as.integer(batch)) -> data_treino

data_treino |> group_by(id, type, model, nodes) |>
  summarize(
    tempo_treino = sum(tempo)/60
  ) |> ungroup() -> data_points


data_treino |>
  group_by(type, model, nodes) |>
  summarize(
    N = n(),
    mean_treino = mean(tempo_treino, na.rm = TRUE),
    sd_treino = sd(tempo_treino),
    se_treino = 3 * sd(tempo_treino) / sqrt(N),
  ) |>
  ungroup() |>
  mutate(
    lower_bound = mean_treino - se_treino,
    upper_bound = mean_treino + se_treino
  ) -> data_treino.plot

data_treino.plot |>
  ggplot(aes(x = as.factor(nodes), y = mean_treino,
             color=model,
             fill=model)) +


  ## geom_line(aes(y = mean_treino, group = model), # <--- Mapeie a linha para a média
  ##         linewidth = 0.3
  ##       , alpha = 1) +

  geom_col(  # Set the transparency (alpha) to 0.3 for a faint look
    position = position_dodge(width = 0.9)
  ) +


  geom_jitter(
    data = data_points,
    aes(x = as.factor(nodes), y = tempo_treino), # Mapeamento X/Y do dado bruto
    # Adicione color=model NO AES() para que o dodge funcione
    # Você já tem fill=model e color=model no mapeamento global.
    # Se você quiser uma cor neutra para os pontos:
    color = "gray30", # Cor fixa fora do aes()
    alpha = 0.3,
    size = 1.5,
    # Use a mesma posição de desvio EXATA
    position = position_jitterdodge(dodge.width = 0.9,
        jitter.width = 0.35)
  ) +
  ## geom_line(linewidth = 1, alpha = 0) +


  geom_errorbar(aes( ymin = lower_bound, ymax = upper_bound),
                width=.8, alpha=.7,
                position = position_dodge(width = 0.9),
                color="black") +

  facet_grid( ~ type) +
  labs(
    y = "Training Time [Minutes]",
    x = "Nodes"
  ) +
  scale_fill_manual(values = c("firebrick2", "#2D72AD")) +
  scale_color_manual(values = c("firebrick2", "#2D72AD")) +

  meu_estilo()
#+end_src

#+CAPTION: Mean of experimental results grouped by node count.
#+NAME:fig:node
#+attr_latex: :placement [!htb] :width \linewidth
#+RESULTS:
[[file:/tmp/babel-IGtNc7/figurecoyPXG.pdf]]


#+begin_src R :results output file graphics  :file (org-babel-temp-file "figure" ".pdf") :exports results :width 8 :height 4 :session *R*

data_plot |> #filter(grepl("*50*", model)) |>
  group_by(id, type, model, batch) |>
  mutate(
    tempo_treino = sum(tempo, na.rm = TRUE) / 60,
  ) |>
  ungroup() |>
  mutate(batch = as.integer(batch)) -> data_treino

data_treino |> group_by(id, type, model, batch) |>
  summarize(
    tempo_treino = sum(tempo)/60
  ) |> ungroup() -> data_points


data_treino |>
  group_by(type, model, batch) |>
  summarize(
    N = n(),
    mean_treino = mean(tempo_treino, na.rm = TRUE),
    sd_treino = sd(tempo_treino),
    se_treino = 3 * sd(tempo_treino) / sqrt(N),
  ) |>
  ungroup() |>
  mutate(
    lower_bound = mean_treino - se_treino,
    upper_bound = mean_treino + se_treino
  ) -> data_treino.plot

data_treino.plot |>
  ggplot(aes(x = as.factor(batch), y = mean_treino,
             color=model,
             fill=model)) +


  ## geom_line(aes(y = mean_treino, group = model), # <--- Mapeie a linha para a média
  ##         linewidth = 0.3
  ##       , alpha = 1) +

  geom_col(  # Set the transparency (alpha) to 0.3 for a faint look
    position = position_dodge(width = 0.9)
  ) +


  geom_jitter(
    data = data_points,
    aes(x = as.factor(batch), y = tempo_treino), # Mapeamento X/Y do dado bruto
    # Adicione color=model NO AES() para que o dodge funcione
    # Você já tem fill=model e color=model no mapeamento global.
    # Se você quiser uma cor neutra para os pontos:
    color = "gray30", # Cor fixa fora do aes()
    alpha = 0.3,
    size = 1.5,
    # Use a mesma posição de desvio EXATA
    position = position_jitterdodge(dodge.width = 0.9,
        jitter.width = 0.35)
  ) +
  ## geom_line(linewidth = 1, alpha = 0) +


  geom_errorbar(aes( ymin = lower_bound, ymax = upper_bound),
                width=.8, alpha=.7,
                position = position_dodge(width = 0.9),
                color="black") +

  facet_grid( ~ type) +
  labs(
    y = "Training Time [Minutes]",
    x = "Batch Size"
  ) +
  scale_fill_manual(values = c("firebrick2", "#2D72AD")) +
  scale_color_manual(values = c("firebrick2", "#2D72AD")) +

  meu_estilo()
#+end_src

#+CAPTION: Mean of experimental results grouped by batch size.
#+NAME:fig:batch
#+attr_latex: :placement [!htb] :width \linewidth
#+RESULTS:
[[file:/tmp/babel-IGtNc7/figureywstTV.pdf]]


*** COMMENT See if the batch affects the one node run
#+begin_src R :results output :session *R* :exports both
data_treino.plot |> select(model, type, batch, nodes, mean_treino) |>
  filter(nodes == 1) |>
  filter(grepl("*B0*", model))
#+end_src

#+RESULTS:
: # A tibble: 5 × 5
:   model          type  batch nodes mean_treino
:   <chr>          <chr> <int> <chr>       <dbl>
: 1 EfficientNetB0 ctl      32 1            26.5
: 2 EfficientNetB0 ctl      64 1            27.7
: 3 EfficientNetB0 sync     32 1            27.4
: 4 EfficientNetB0 sync     64 1            28.4
: 5 EfficientNetB0 sync    100 1            29.0


*** COMMENT Modeling a linear model
We propose a linear model to extrapolate our results and predict the
training time. As the training time is almost constant for one node,
we decided to just create the model based on the parallel executions,
meaning that executions with more then one node were used to the model
creation. For each combination of model and type we created a
different model.

Figure 4 and 5 show a plot of our two proposed models based on the
nodes count and the batch size. In the first model the batch
size as a quadratic coefficient, represent the impact viewed in
Figure 3. The second show a linear model


#+begin_src R :results output file graphics :file (org-babel-temp-file "figure" ".pdf") :exports results :width 8 :height 4 :session *R*
library(ggplot2)
library(dplyr)
library(tidyr)

grid_predicao <- expand.grid(
  batch = seq(from = 32, to = 100, length.out = 100),
  nodes = unique(data.fit$nodes),
  model = unique(data.fit$model),
  type  = unique(data.fit$type)
)

grid_predicao$tempo_predito <- NA

combinacoes <- data.fit |> distinct(model, type)

for(i in 1:nrow(combinacoes)) {

  m_atual <- combinacoes$model[i]
  t_atual <- combinacoes$type[i]

  dados_grupo <- data.fit |> filter(model == m_atual, type == t_atual)

  if(nrow(dados_grupo) < 3) next

  # Modelo quadratico baseado on tamanho do batch
  modelo <- lm(tempo_treino ~ batch + as.factor(nodes) + I(batch^2), data = dados_grupo)

  idx_grid <- grid_predicao$model == m_atual & grid_predicao$type == t_atual

  grid_predicao$tempo_predito[idx_grid] <- predict(modelo, newdata = grid_predicao[idx_grid, ])
}

grid_predicao <- grid_predicao |> filter(!is.na(tempo_predito))

ggplot() +
  geom_point(data = data.fit, aes(x = batch, y = tempo_treino, color = as.factor(nodes)), alpha = 0.6) +

  geom_line(data = grid_predicao, aes(x = batch, y = tempo_predito, color = as.factor(nodes)), size = 1) +

  facet_grid(type ~ model) +

  labs(
    x = "Batch Size",
    y = "Training Time [Seconds]",
    color = "Nodos"
  ) +

  scale_fill_manual(values = c("firebrick2", "#2D72AD")) +
  scale_color_manual(values = c("firebrick2", "#2D72AD")) +
  meu_estilo()
#+end_src

#+CAPTION: Quadratic model
#+NAME:fig:modQuad
#+attr_latex: :placement [!htb] :width \linewidth
#+RESULTS:
[[file:/tmp/babel-IGtNc7/figureSVVYgA.pdf]]


#+begin_src R :results output file graphics :file (org-babel-temp-file "figure" ".pdf") :exports results :width 8 :height 4 :session *R*
library(ggplot2)
library(dplyr)
library(tidyr)

grid_predicao <- expand.grid(
  batch = seq(from = 32, to = 100, length.out = 100),
  nodes = unique(data.fit$nodes),
  model = unique(data.fit$model),
  type  = unique(data.fit$type)
)

grid_predicao$tempo_predito <- NA

combinacoes <- data.fit |> distinct(model, type)

for(i in 1:nrow(combinacoes)) {

  m_atual <- combinacoes$model[i]
  t_atual <- combinacoes$type[i]

  dados_grupo <- data.fit |> filter(model == m_atual, type == t_atual)

  if(nrow(dados_grupo) < 3) next

  # Modelo linear baseado on tamanho do batch
  modelo <- lm(tempo_treino ~ batch + as.factor(nodes), data = dados_grupo)

  idx_grid <- grid_predicao$model == m_atual & grid_predicao$type == t_atual

  grid_predicao$tempo_predito[idx_grid] <- predict(modelo, newdata = grid_predicao[idx_grid, ])
}

grid_predicao <- grid_predicao |> filter(!is.na(tempo_predito))

ggplot() +
  geom_point(data = data.fit, aes(x = batch, y = tempo_treino, color = as.factor(nodes)), alpha = 0.6) +

  geom_line(data = grid_predicao, aes(x = batch, y = tempo_predito, color = as.factor(nodes)), size = 1) +

  facet_grid(type ~ model) +

  labs(
    x = "Batch Size",
    y = "Training Time [Seconds]",
    color = "Nodos"
  ) +

  scale_fill_manual(values = c("firebrick2", "#2D72AD")) +
  scale_color_manual(values = c("firebrick2", "#2D72AD")) +
  meu_estilo()
#+end_src

#+CAPTION: Linear Model
#+NAME:fig:modLin
#+attr_latex: :placement [!htb] :width \linewidth
#+RESULTS:
[[file:/tmp/babel-IGtNc7/figurezjLCMc.pdf]]

** Conclusion

We could conclude that the network and the model size have a huge
importance in the distributed training, but that even with a network
with a low throughput, it is possible to gain performance when
training by correctly configuring the environment and model
parameters. As future work we intend to deep investigate the impact of the network
and the model size in the distributed training.

** Acknowledgments                                                  :ignore:

#+LATEX:\section*{Acknowledgements}
We would like to express our gratitude to Professor Dr. Lucas Mello
Schnorr for his guidance and the valuable insights shared regarding
performance analysis.  We also would like to thank the PCAD at
INF/UFRGS for making infrastructure and hardware used in the
experiments available.


** References                                                        :ignore:

# See next section to understand how refs.bib file is created.
bibliographystyle:sbc.bst
[[bibliography:refs.bib]]

* Bib file is here                                                 :noexport:

Tangle this file with C-c C-v t

#+begin_src bib :tangle refs.bib
@inproceedings{tensorflow2016,
  author    = {Abadi, Mart\'{i}n and Barham, Paul and Chen, Jianmin and Chen, Zhifeng and Davis, Andy and Dean, Jeffrey and Devin, Matthieu and Ghemawat, Sanjay and Irving, Geoffrey and Isard, Michael and Kudlur, Manjunath and Levenberg, Josh and Monga, Rajat and Moore, Sherry and Murray, Derek G. and Steiner, Benoit and Tucker, Paul and Vasudevan, Vijay and Warden, Pete and Wicke, Martin and Yu, Yuan and Zheng, Xiaoqiang},
  title     = {TensorFlow: A System for Large-Scale Machine Learning},
  booktitle = {Proceedings of the 12th USENIX Symposium on Operating Systems Design and Implementation (OSDI '16)},
  year      = {2016},
  publisher = {USENIX Association},
}

@inproceedings{yoo2003slurm,
  title={SLURM: Simple Linux Utility for Resource Management},
  author={Yoo, Andy B. and Jette, Morris A. and Grondona, Mark},
  booktitle={Job Scheduling Strategies for Parallel Processing},
  pages={44--60},
  year={2003},
  publisher={Springer} ,
  doi={10.1007/10968987_3}
}

@inproceedings{courtes2013guix,
  title={Functional Package Management with Guix},
  author={Court{\`e}s, Ludovic},
  booktitle={Proceedings of the 6th European Lisp Symposium (ELS)},
  pages={15--28},
  year={2013},
  url={https://arxiv.org/abs/1305.4584}
}

@article{efficientNet,
  author       = {Mingxing Tan and
                  Quoc V. Le},
  title        = {EfficientNet: Rethinking Model Scaling for Convolutional Neural Networks},
  journal      = {CoRR},
  volume       = {abs/1905.11946},
  year         = {2019},
  url          = {http://arxiv.org/abs/1905.11946},
  eprinttype    = {arXiv},
  eprint       = {1905.11946},
  timestamp    = {Mon, 03 Jun 2019 13:42:33 +0200},
  biburl       = {https://dblp.org/rec/journals/corr/abs-1905-11946.bib},
  bibsource    = {dblp computer science bibliography, https://dblp.org}
}

@article{resnet,
  author       = {Kaiming He and
                  Xiangyu Zhang and
                  Shaoqing Ren and
                  Jian Sun},
  title        = {Deep Residual Learning for Image Recognition},
  journal      = {CoRR},
  volume       = {abs/1512.03385},
  year         = {2015},
  url          = {http://arxiv.org/abs/1512.03385},
  eprinttype    = {arXiv},
  eprint       = {1512.03385},
  timestamp    = {Wed, 25 Jan 2023 11:01:16 +0100},
  biburl       = {https://dblp.org/rec/journals/corr/HeZRS15.bib},
  bibsource    = {dblp computer science bibliography, https://dblp.org}
}

@inproceedings{imagenet,
  title={Imagenet: A large-scale hierarchical image database},
  author={Deng, Jia and Dong, Wei and Socher, Richard and Li, Li-Jia and Li, Kai and Fei-Fei, Li},
  booktitle={2009 IEEE conference on computer vision and pattern recognition},
  pages={248--255},
  year={2009},
  organization={Ieee}
}

@misc{distributed,
  author = {TensorFlow Documentation},
  title = {Distributed training with Keras},
  year=2023,
  howpublished = {\url{https://www.tensorflow.org/tutorials/distribute/keras}},
  note = {Accessed: 2025-11-30}
}
#+end_src

* Emacs setup                                                      :noexport:
# Local Variables:
# eval: (add-to-list 'load-path ".")
# eval: (require 'ox-extra)
# eval: (ox-extras-activate '(ignore-headlines))
# eval: (require 'org-ref)
# eval: (require 'doi-utils)
# eval: (add-to-list 'org-latex-packages-alist '("" "url") t)
# eval: (add-to-list 'org-latex-packages-alist '("" "sbc-template") t)
# eval: (add-to-list 'org-latex-packages-alist '("AUTO" "babel" t ("pdflatex")))
# eval: (setq org-latex-pdf-process (list "latexmk -pdf %f"))
# eval: (add-to-list 'org-export-before-processing-hook (lambda (be) (org-babel-tangle)))
# End:
