#+STARTUP: content
#+STARTUP: overview
#+STARTUP: indent
#+TITLE: DL parallel training
#+AUTHOR: Rayan Raddatz de Matos

* Installation requirements
:PROPERTIES:
:header-args: :tangle create-env.sh :tangle-mode (identity #o755) :shebang "#!/usr/bin/bash"
:END:
** Create venv

#+begin_src shell :results output :exports both
python3 -m venv venv

. venv/bin/activate
which python

python3 --version
pip --version
#+end_src

#+RESULTS:
: /home/rayan/graduation/estudos/perf/final/tensorflow/venv/bin/python
: Python 3.11.2
: pip 25.2 from /home/rayan/graduation/estudos/perf/final/tensorflow/venv/lib/python3.11/site-packages/pip (python 3.11)

** Install packages

With the previous virtual environment activated, install the required
packages. Also freeze to save the exact used versions.

#+begin_src shell :results output :exports both
pip install --upgrade pip
pip install tensorflow==2.15.1
pip install tensorflow[and-gpu]==2.15.1
pip install tensorflow[and-cuda]==2.15.1
pip install pandas # By default already installed, but need for poti for an unknow reason

pip freeze > reqs.txt
#+end_src

#+RESULTS:

** Verify installation

Verify if TensorFlow is working and print the requirements.

#+begin_src shell :results output :exports both
python3 -c "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))"
cat reqs.txt
#+end_src

#+RESULTS:
#+begin_example
appdirs==1.4.4
arandr==0.1.11
asciinema==2.2.0
attrs==22.2.0
beautifulsoup4==4.11.2
beniget==0.4.1
blinker==1.9.0
Brlapi==0.8.4
Brotli==1.0.9
certifi==2025.8.3
cffi==2.0.0
chardet==5.1.0
charset-normalizer==3.4.3
click==8.2.1
contourpy==1.0.7
crit==3.17.1
cryptography==38.0.4
cupshelpers==1.0
cycler==0.11.0
dblatex==0.3.12
dbus-python==1.3.2
decorator==5.1.1
Deprecated==1.2.18
distro==1.8.0
distro-info==1.5+deb12u1
docker==5.0.3
docker-compose==1.29.2
dockerpty==0.4.1
docopt==0.6.2
epc==0.0.5
exif==1.6.1
Flask==3.1.2
fonttools==4.38.0
fs==2.4.16
gast==0.5.2
giturlparse==0.12.0
gpg==1.18.0
gyp==0.1
html5lib==1.1
httplib2==0.20.4
idna==3.3
itsdangerous==2.2.0
Jinja2==3.1.6
jsonpointer==2.3
jsonschema==4.10.3
kiwisolver==0.0.0
lazr.restfulclient==0.14.5
lazr.uri==1.0.6
louis==3.24.0
lxml==6.0.1
lz4==4.0.2+dfsg
MarkupSafe==3.0.2
matplotlib==3.6.3
mercurial==6.3.2
meson==1.0.1
mpmath==0.0.0
mutagen==1.47.0
networkx==2.8.8
numpy==2.3.3
oauthlib==3.2.2
olefile==0.46
packaging==23.0
pillow==11.3.0
plum-py==0.8.7
ply==3.11
protobuf==4.21.12
psutil==7.0.0
pyasn1==0.6.1
pycairo==1.20.1
pycparser==2.23
pycryptodome==3.23.0
pycups==2.0.1
pycurl==7.45.6
pygit2==1.18.2
Pygments==2.19.2
PyGObject==3.42.2
PyJWT==2.6.0
pyparsing==3.0.9
pypinyin==0.55.0
pypng==0.20220715.0
PyQt6==6.5.0
PyQt6-Qt6==6.5.0
PyQt6-WebEngine==6.5.0
PyQt6-WebEngine-Qt6==6.5.0
PyQt6_sip==13.10.2
pyrsistent==0.18.1
PySimpleSOAP==1.16.2
pysmbc==1.0.23
PySocks==1.7.1
pytaglib==3.0.1
python-apt==2.6.0
python-dateutil==2.8.2
python-debian==0.1.49
python-debianbts==4.0.1
python-dotenv==0.21.0
python_tsp==0.5.0
pythran==0.11.0
pytz==2022.7.1
pywal==3.3.0
pyxdg==0.28
PyYAML==6.0
qrcode==7.4.2
reportbug==12.0.0
requests==2.32.5
rfc3987==1.3.8
rsa==4.9.1
scipy==1.10.1
SCons==4.4.0
sexpdata==1.0.0
six==1.16.0
soupsieve==2.3.2
sympy==1.11.1
tabulate==0.8.10
texttable==1.6.7
tld==0.13.1
tsplib95==0.7.1
typing_extensions==4.4.0
ufoLib2==0.14.0
unidiff==0.7.5
uritemplate==4.1.1
urllib3==1.26.12
wadllib==1.3.6
webcolors==1.11.1
webencodings==0.5.1
websocket-client==1.2.3
Werkzeug==3.1.3
wrapt==1.17.3
xdg==5
xdot==1.2
#+end_example

* Synchronous parallel training
:PROPERTIES:
:header-args: :tangle train-sync.py :tangle-mode (identity #o755)
:END:
** Libraries
Needed libraries

#+begin_src python :session *P* :results output :exports both
import tensorflow as tf
from tensorflow.keras.utils import to_categorical
from tensorflow.keras.applications import ResNet50
from tensorflow.keras.models import Model
from tensorflow.keras.layers import Dense, GlobalAveragePooling2D
from tensorflow.keras.optimizers import SGD
import os
import json
import time
from datetime import datetime
import pathlib
import pandas as pd
#+end_src

** TensorFlow Callbacks

#+begin_src python :session *P* :results output :exports both
log_dir = "logs/profile/" + datetime.now().strftime("%Y%m%d-%H%M%S")

tensorboard_callback = tf.keras.callbacks.TensorBoard(
    log_dir=log_dir,
    histogram_freq=1,
    profile_batch='2,10'  # Profile batches 100 through 105
)
#+end_src

** Hyper parameters and configurations (cluster and training)

Here are the run configurations but also the " tf config" configurations
#+begin_src python :session *P* :results output :exports both
strategy = tf.distribute.MultiWorkerMirroredStrategy()
tf_config_str = os.environ.get('TF_CONFIG', '{}')
tf_config = json.loads(tf_config_str)
task_info = tf_config.get('task', {})
worker_id = task_info.get('index', 0)

SEED = 1
IMG_SIZE = (224, 224)
EPOCHS = 5
BATCH_SIZE_PER_REPLICA = 96
NUM_CLASSES = 200
NUM_TRAIN_IMAGES = 100000

tf.random.set_seed(SEED)

NUM_WORKERS = strategy.num_replicas_in_sync
GLOBAL_BATCH_SIZE = BATCH_SIZE_PER_REPLICA * NUM_WORKERS

print(f"Worker ID: {worker_id}")
print(f"Número total de workers (réplicas): {NUM_WORKERS}")
print(f"Batch size por worker: {BATCH_SIZE_PER_REPLICA}")
print(f"Batch size global (total): {GLOBAL_BATCH_SIZE}\n")
#+end_src

** TODO Dataset load and config
Here is where the dataset is loaded, its needed to optimize this to
the parallel environment.

https://www.tensorflow.org/guide/data_performance
#+begin_src python :session *P* :results output :exports both
def load_tiny_imagenet_datasets(data_path, num_classes):
    train_path = data_path / 'train'
    val_path = data_path / 'val'

    train_dataset = tf.keras.utils.image_dataset_from_directory(
        train_path,
        labels='inferred',
        label_mode='categorical',
        image_size=(64, 64),
        interpolation='nearest',
        batch_size=None,
        shuffle=False
    )

    val_annotations_path = val_path / 'val_annotations.txt'
    val_data = pd.read_csv(val_annotations_path, sep='\t', header=None, names=['File', 'Class', 'X1', 'Y1', 'X2', 'Y2'])

    class_names = sorted(os.listdir(train_path))
    class_to_idx = {name: index for index, name in enumerate(class_names)}

    val_images = [str(val_path / 'images' / fname) for fname in val_data['File']]
    val_labels_str = val_data['Class'].values
    val_labels_int = [class_to_idx[name] for name in val_labels_str]
    val_labels_cat = to_categorical(val_labels_int, num_classes=num_classes)

    val_dataset = tf.data.Dataset.from_tensor_slices((val_images, val_labels_cat))

    def parse_image(filename, label):
        image = tf.io.read_file(filename)
        image = tf.image.decode_jpeg(image, channels=3)
        return image, label

    val_dataset = val_dataset.map(parse_image, num_parallel_calls=tf.data.AUTOTUNE)
    return train_dataset, val_dataset


def preprocess_image(image, label):
    image = tf.cast(image, tf.float32) / 255.0
    image = tf.image.resize(image, IMG_SIZE)
    return image, label


print("Carregando o dataset Tiny ImageNet de um diretório local...")
tiny_imagenet_path = pathlib.Path('/scratch/rrdmatos/tiny-imagenet-200') # Set here the path to the dataset
train_dataset, test_dataset = load_tiny_imagenet_datasets(tiny_imagenet_path, NUM_CLASSES)

options = tf.data.Options()
options.experimental_distribute.auto_shard_policy = tf.data.experimental.AutoShardPolicy.DATA

train_dataset = train_dataset.map(preprocess_image, num_parallel_calls=tf.data.AUTOTUNE)
train_dataset = train_dataset.with_options(options)
train_dataset = train_dataset.shuffle(NUM_TRAIN_IMAGES, seed=SEED)
train_dataset = train_dataset.batch(GLOBAL_BATCH_SIZE)
train_dataset = train_dataset.prefetch(tf.data.AUTOTUNE)
train_dataset = train_dataset.cache()

test_dataset = test_dataset.map(preprocess_image, num_parallel_calls=tf.data.AUTOTUNE)
test_dataset = test_dataset.cache()
test_dataset = test_dataset.with_options(options)
test_dataset = test_dataset.batch(GLOBAL_BATCH_SIZE).prefetch(tf.data.AUTOTUNE)
#+end_src

** Build and compile the model

Using the strategy to create compile the model
#+begin_src python :session *P* :results output :exports both
with strategy.scope():
    input_shape = (224, 224, 3)
    base_model = ResNet50(weights=None, include_top=False, input_shape=input_shape)
    base_model.trainable = True

    x = GlobalAveragePooling2D()(base_model.output)
    x = Dense(1024, activation="relu")(x)
    predictions = Dense(NUM_CLASSES, activation="softmax")(x)

    model = Model(inputs=base_model.input, outputs=predictions)

    BASE_LEARNING_RATE = 0.01
    SCALED_LEARNING_RATE = BASE_LEARNING_RATE * NUM_WORKERS

    opt = SGD(learning_rate=SCALED_LEARNING_RATE, weight_decay=0.0001, momentum=0.9)

    model.compile(optimizer=opt, loss="categorical_crossentropy", metrics=["accuracy"])
#+end_src

** Actual training

Here is where the training really happens, we are using the model.fit
function.
#+begin_src python :session *P* :results output :exports both
duration_seconds = 0
print("Iniciando o treinamento distribuído...")

start_train_time = time.perf_counter()
history = model.fit(train_dataset, epochs=EPOCHS, validation_data=test_dataset, callbacks=[tensorboard_callback])
close_train_time = time.perf_counter()

duration_seconds = close_train_time - start_train_time

score = model.evaluate(test_dataset, verbose=(1 if worker_id == 0 else 0))
if worker_id == 0:
    print("\nIniciando avaliação final no worker 0...")
    print(f"Loss (perda) no teste: {score[0]:.4f}")
    print(f"Accuracy (acurácia) no teste: {score[1]:.4f}")

print("TIME TRAINING:", duration_seconds)
print(f"\nWorker {worker_id} concluiu.")
#+end_src


* Examples of experimental design and running scripts
** Creating an experimental design

#+begin_src R :results output :session *R* :exports both :noweb yes :colnames yes
options(crayon.enabled=FALSE)
#install.packages('DoE.base', dependencies = TRUE, repos='http://cran.rstudio.com/')
library(DoE.base)
library(tidyverse)

fator_experiment = c("no_trace", "trace", "empty", "rast")
fator_size = c(1024, 2048, 4096)
fator_blocksize = c(32, 64, 128, 256)
fator_threads = c(16, 32)

fac.design(nfactors=4,
           replications=30,
           repeat.only= FALSE ,
           randomize=TRUE,
           seed=17366,
           nlevels=c(length(fator_experiment),
                     length(fator_size),
                     length(fator_blocksize),
                     length(fator_threads)),
           factor.names=list(
             experiment = fator_experiment,
             size = fator_size,
             block_size = fator_blocksize,
             threads = fator_threads
           )) |>
  as_tibble() |>
  mutate_at(vars(size:threads), as.character) |>
  mutate_at(vars(size:threads), as.numeric) |>
  #filter(threads == 16) |>
  mutate(razao  = size/block_size) |>
  filter(razao >= 8 & razao <= 64) |>
  print() |>
  write_csv("h.csv", progress=FALSE)
  read_csv("h.csv", progress=FALSE, show_col_types=FALSE) |>
  print(n=100)

#+end_src

#+RESULTS:
#+begin_example
creating full factorial with 96 runs ...

# A tibble: 2,400 × 6
   experiment  size block_size threads Blocks razao
   <
     <
     <
  <
<
 <dbl>
 1 no_trace    2048        256      32 .1         8
 2 no_trace    1024         64      16 .1        16
 3 trace       2048        128      16 .1        16
 4 trace       4096        256      16 .1        16
 5 trace       2048         64      16 .1        32
 6 no_trace    2048        128      16 .1        16
 7 no_trace    2048         32      16 .1        64
 8 rast        4096         64      32 .1        64
 9 trace       4096        256      32 .1        16
10 empty       4096         64      16 .1        64
# … with 2,390 more rows
# A tibble: 2,400 × 6
    experiment  size block_size threads Blocks razao
    <
     <
     <
  <
 <
<dbl>
  1 no_trace    2048        256      32    0.1     8
  2 no_trace    1024         64      16    0.1    16
  3 trace       2048        128      16    0.1    16
  4 trace       4096        256      16    0.1    16
  5 trace       2048         64      16    0.1    32
  6 no_trace    2048        128      16    0.1    16
  7 no_trace    2048         32      16    0.1    64
  8 rast        4096         64      32    0.1    64
  9 trace       4096        256      32    0.1    16
 10 empty       4096         64      16    0.1    64
 11 no_trace    4096        128      32    0.1    32
 12 rast        2048        128      32    0.1    16
 13 no_trace    2048         64      32    0.1    32
 14 rast        1024        128      32    0.1     8
 15 no_trace    1024        128      32    0.1     8
 16 trace       4096         64      16    0.1    64
 17 trace       1024         64      32    0.1    16
 18 empty       2048         64      32    0.1    32
 19 rast        1024         32      32    0.1    32
 20 rast        2048        128      16    0.1    16
 21 trace       1024         32      16    0.1    32
 22 trace       4096         64      32    0.1    64
 23 trace       4096        128      32    0.1    32
 24 rast        1024         32      16    0.1    32
 25 trace       1024         32      32    0.1    32
 26 rast        4096        256      16    0.1    16
 27 rast        2048         32      16    0.1    64
 28 no_trace    4096         64      32    0.1    64
 29 rast        4096         64      16    0.1    64
 30 empty       1024         32      16    0.1    32
 31 rast        4096        128      16    0.1    32
 32 no_trace    1024         32      16    0.1    32
 33 no_trace    2048        256      16    0.1     8
 34 empty       1024        128      16    0.1     8
 35 no_trace    2048         64      16    0.1    32
 36 rast        1024         64      16    0.1    16
 37 empty       4096        128      32    0.1    32
 38 no_trace    2048         32      32    0.1    64
 39 trace       2048         32      16    0.1    64
 40 no_trace    4096        128      16    0.1    32
 41 no_trace    4096        256      32    0.1    16
 42 empty       1024         64      32    0.1    16
 43 trace       2048        128      32    0.1    16
 44 empty       2048         32      32    0.1    64
 45 empty       1024         32      32    0.1    32
 46 empty       2048        128      16    0.1    16
 47 no_trace    4096        256      16    0.1    16
 48 no_trace    1024         32      32    0.1    32
 49 rast        1024        128      16    0.1     8
 50 rast        1024         64      32    0.1    16
 51 trace       1024        128      16    0.1     8
 52 trace       1024         64      16    0.1    16
 53 empty       2048        256      16    0.1     8
 54 trace       4096        128      16    0.1    32
 55 rast        4096        256      32    0.1    16
 56 no_trace    1024        128      16    0.1     8
 57 no_trace    4096         64      16    0.1    64
 58 rast        2048        256      16    0.1     8
 59 rast        2048        256      32    0.1     8
 60 empty       2048         64      16    0.1    32
 61 trace       2048         32      32    0.1    64
 62 trace       1024        128      32    0.1     8
 63 rast        2048         32      32    0.1    64
 64 rast        2048         64      16    0.1    32
 65 empty       2048        256      32    0.1     8
 66 empty       2048         32      16    0.1    64
 67 no_trace    1024         64      32    0.1    16
 68 empty       4096        256      16    0.1    16
 69 rast        2048         64      32    0.1    32
 70 empty       1024        128      32    0.1     8
 71 no_trace    2048        128      32    0.1    16
 72 trace       2048         64      32    0.1    32
 73 empty       1024         64      16    0.1    16
 74 empty       2048        128      32    0.1    16
 75 trace       2048        256      32    0.1     8
 76 empty       4096        256      32    0.1    16
 77 rast        4096        128      32    0.1    32
 78 empty       4096         64      32    0.1    64
 79 trace       2048        256      16    0.1     8
 80 empty       4096        128      16    0.1    32
 81 empty       4096        128      32    0.2    32
 82 rast        2048        128      32    0.2    16
 83 trace       4096        128      16    0.2    32
 84 rast        1024         64      32    0.2    16
 85 rast        1024         64      16    0.2    16
 86 empty       1024         32      16    0.2    32
 87 rast        2048         64      32    0.2    32
 88 empty       2048        256      32    0.2     8
 89 trace       1024        128      32    0.2     8
 90 no_trace    1024        128      16    0.2     8
 91 empty       2048        128      32    0.2    16
 92 no_trace    2048         64      16    0.2    32
 93 empty       2048         64      32    0.2    32
 94 trace       1024         32      32    0.2    32
 95 empty       2048        128      16    0.2    16
 96 trace       2048         32      16    0.2    64
 97 empty       4096        256      16    0.2    16
 98 no_trace    4096        128      32    0.2    32
 99 no_trace    1024         64      16    0.2    16
100 empty       2048         64      16    0.2    32
# … with 2,300 more rows
#+end_example

** Running an experiment
*** Running the full experiment

#+begin_src shell :results output :exports both :tangle run-experiment.sh :tangle-mode (identity #o755) :shebang "#!/usr/bin/bash"

# https://gist.github.com/mohanpedala/1e2ff5661761d3abd0385e8223e16425
set -euxo pipefail

informar_ajuda() {
    echo "$0 <NAME> <PROJECT> <REPETITION_PER_CASE>"
}

NAME=${1:-}
if [[ -z "${NAME}" ]]; then
    informar_ajuda
    exit 1
fi

PROJECT=${2:-}
if [[ -z "${PROJECT}" ]]; then
    informar_ajuda
    exit 1
fi

REPETITION=${3:-}
if [[ -z "${REPETITION}" ]]; then
    REPETITION=1
fi


echo "Lapacke Version 3.12.0"
which clang

EXP="EXP-${NAME}"
mkdir -p ${EXP}

cp -r application $EXP
cp -r scripts $EXP
cp OMPT_Tracer.org $EXP

mkdir -p "${EXP}/scripts"

cp ./run-project.sh ./run-case.sh ${EXP}
cp ${PROJECT} "${EXP}/${EXP}-project.csv"

pushd $EXP
{
./run-project.sh "${EXP}-project.csv" ${REPETITION} > "${EXP}-project-run.log"
mv "logs-${EXP}-project.csv" "run-logs-${EXP}"
mv "resultados_projeto.csv" "results-${EXP}.csv"
}
popd

cp ${0} ${EXP}

#+end_src

*** Run the experimental design

1. Ler o projeto experimental no formato CSV
2. Cada linha terá o script abaixo (run-project.sh) executado

#+begin_src shell :results output :exports both :tangle run-project.sh :tangle-mode (identity #o755) :shebang "#!/usr/bin/bash"

# fail on error
# https://gist.github.com/mohanpedala/1e2ff5661761d3abd0385e8223e16425
set -euxo pipefail

informar_ajuda() {
    #                   REPS for each run
    echo "$0 <PROJECT> <REPETITION>"
}

PROJECT=${1:-}
if [[ -z "${PROJECT}" ]]; then
    informar_ajuda
    exit 1
fi

REPETITION=${2:-}
if [[ -z "${REPETITION}" ]]; then
    REPETITION=1
fi

# Initiating necessary data ----------------
OUTPUTFILE="resultados_projeto.csv"
echo "run,experiment,app_time,comp_time,operations,size,blocksize,threads" > ${OUTPUTFILE}

LOGS="logs-${PROJECT}"
mkdir -p $LOGS

FILE=$(tail -n +2 $PROJECT) # remove header

RUNS=0
echo "$FILE" | while IFS=, read -r TYPE SIZE BLOCK THREADS B; do

    # Running ----------------
    ./run-case.sh $TYPE $SIZE $BLOCK $REPETITION $THREADS
    RESULTS=$(cat times.csv | tail -n +2)
    RUNS=$(($RUNS+1))

    # Saving Data --------------
    echo "${RUNS},${RESULTS}" >> $OUTPUTFILE
    OUTDIR="${TYPE}-${SIZE}-${BLOCK}"
    mv $OUTDIR "${LOGS}/${RUNS}-${OUTDIR}"

    # Removing unnecessary info ----------
    rm -f times.csv

done


#+end_src

#+RESULTS:

*** Run one case and generate CSV data for analysis

Para fazer o tangle deste trecho de código no arquivo especificado no ":tangle"

C-c C-v t

#+begin_src shell :results output :exports both :tangle run-case.sh :tangle-mode (identity #o755) :shebang "#!/usr/bin/bash"
# fail on error
# https://gist.github.com/mohanpedala/1e2ff5661761d3abd0385e8223e16425
set -euxo pipefail

informar_ajuda() {
    echo "$0 <EXPERIMENT> <SIZE> <BLOCK> <REPETITION> <THREADS>"
}

EXPERIMENT=${1:-}
if [[ -z "${EXPERIMENT}" ]]; then
    informar_ajuda
    exit 1
fi

SIZE=${2:-}
if [[ -z "${SIZE}" ]]; then
    informar_ajuda
    exit 1
fi

BLOCK=${3:-}
if [[ -z "${BLOCK}" ]]; then
    informar_ajuda
    exit 1
fi

REPETITION=${4:-}
if [[ -z "${REPETITION}" ]]; then
    informar_ajuda
    exit 1
fi

THR=${5:-}
if [[ -z "${THR}" ]]; then
    THR=4
fi

HERE=$(pwd)

# Set Enviroment variables -------------
export OMP_NUM_THREADS=$THR
export OMP_PLACES='threads(${THR})'
export OMP_PROC_BIND=true
export OMP_DYNAMIC=false
export OMP_DISPLAY_ENV=verbose
export OMP_TOOL_LIBRARIES="$HOME/graduation/ic/ompt/application/lib${EXPERIMENT}.so"

export CFLAGS=-I$HOME/tmp/install/lapack-git/include
export LDFLAGS=-L$HOME/tmp/install/lapack-git/lib
export LD_LIBRARY_PATH=$HOME/tmp/install/lapack-git/lib:$HOME/graduation/ic/ompt/application:$HOME/tmp/install/librastro/lib:$HERE:$HERE/application
export LAPACKE_NANCHECK=0

export ASAN_OPTIONS=intercept_tls_get_addr=0
export ASAN_OPTIONS=detect_odr_violation=0

export PATH=$PATH:$HOME/tmp/install

TEMPFILE=$(mktemp)

pushd application
{
    make clean
    make ${EXPERIMENT}
}
popd

./scripts/run-many.sh ${REPETITION} ${SIZE} ${BLOCK} 0 2
OUTPUTDIR="${EXPERIMENT}-${SIZE}-${BLOCK}"
mv -v runs ${OUTPUTDIR}
./scripts/make-csv.sh ${OUTPUTDIR} "${EXPERIMENT}"  | \
    sed "s/$/,${SIZE},${BLOCK},${THR}/" >> ${TEMPFILE}

OUTPUTFILE=times.csv
echo "experiment,app_time,comp_time,operations,size,blocksize,threads" > ${OUTPUTFILE}
cat ${TEMPFILE} >> ${OUTPUTFILE}
rm -f ${TEMPFILE}

#+end_src

#+RESULTS:
