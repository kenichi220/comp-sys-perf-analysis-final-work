#+STARTUP: content
#+STARTUP: overview
#+STARTUP: indent
#+TITLE: DL parallel training
#+AUTHOR: Rayan Raddatz de Matos

* Installation requirements
:PROPERTIES:
:header-args: :tangle create-env.sh :tangle-mode (identity #o755) :shebang "#!/usr/bin/bash"
:END:
** Create venv

#+begin_src shell :results output :exports both
python3 -m venv venv

. venv/bin/activate
which python

python3 --version
pip --version
#+end_src

#+RESULTS:
: /home/rayan/graduation/estudos/perf/final/tensorflow/venv/bin/python
: Python 3.11.2
: pip 25.2 from /home/rayan/graduation/estudos/perf/final/tensorflow/venv/lib/python3.11/site-packages/pip (python 3.11)

** Install packages

With the previous virtual environment activated, install the required
packages. Also freeze to save the exact used versions.

#+begin_src shell :results output :exports both
pip install --upgrade pip
pip install tensorflow==2.15.1
pip install tensorflow[and-gpu]==2.15.1
pip install tensorflow[and-cuda]==2.15.1
pip install pandas # By default already installed, but need for poti for an unknow reason

pip freeze > reqs.txt
#+end_src

#+RESULTS:

** Verify installation

Verify if TensorFlow is working and print the requirements.

#+begin_src shell :results output :exports both
python3 -c "import tensorflow as tf; print(tf.config.list_physical_devices('GPU'))"
cat reqs.txt
#+end_src

#+RESULTS:
#+begin_example
appdirs==1.4.4
arandr==0.1.11
asciinema==2.2.0
attrs==22.2.0
beautifulsoup4==4.11.2
beniget==0.4.1
blinker==1.9.0
Brlapi==0.8.4
Brotli==1.0.9
certifi==2025.8.3
cffi==2.0.0
chardet==5.1.0
charset-normalizer==3.4.3
click==8.2.1
contourpy==1.0.7
crit==3.17.1
cryptography==38.0.4
cupshelpers==1.0
cycler==0.11.0
dblatex==0.3.12
dbus-python==1.3.2
decorator==5.1.1
Deprecated==1.2.18
distro==1.8.0
distro-info==1.5+deb12u1
docker==5.0.3
docker-compose==1.29.2
dockerpty==0.4.1
docopt==0.6.2
epc==0.0.5
exif==1.6.1
Flask==3.1.2
fonttools==4.38.0
fs==2.4.16
gast==0.5.2
giturlparse==0.12.0
gpg==1.18.0
gyp==0.1
html5lib==1.1
httplib2==0.20.4
idna==3.3
itsdangerous==2.2.0
Jinja2==3.1.6
jsonpointer==2.3
jsonschema==4.10.3
kiwisolver==0.0.0
lazr.restfulclient==0.14.5
lazr.uri==1.0.6
louis==3.24.0
lxml==6.0.1
lz4==4.0.2+dfsg
MarkupSafe==3.0.2
matplotlib==3.6.3
mercurial==6.3.2
meson==1.0.1
mpmath==0.0.0
mutagen==1.47.0
networkx==2.8.8
numpy==2.3.3
oauthlib==3.2.2
olefile==0.46
packaging==23.0
pillow==11.3.0
plum-py==0.8.7
ply==3.11
protobuf==4.21.12
psutil==7.0.0
pyasn1==0.6.1
pycairo==1.20.1
pycparser==2.23
pycryptodome==3.23.0
pycups==2.0.1
pycurl==7.45.6
pygit2==1.18.2
Pygments==2.19.2
PyGObject==3.42.2
PyJWT==2.6.0
pyparsing==3.0.9
pypinyin==0.55.0
pypng==0.20220715.0
PyQt6==6.5.0
PyQt6-Qt6==6.5.0
PyQt6-WebEngine==6.5.0
PyQt6-WebEngine-Qt6==6.5.0
PyQt6_sip==13.10.2
pyrsistent==0.18.1
PySimpleSOAP==1.16.2
pysmbc==1.0.23
PySocks==1.7.1
pytaglib==3.0.1
python-apt==2.6.0
python-dateutil==2.8.2
python-debian==0.1.49
python-debianbts==4.0.1
python-dotenv==0.21.0
python_tsp==0.5.0
pythran==0.11.0
pytz==2022.7.1
pywal==3.3.0
pyxdg==0.28
PyYAML==6.0
qrcode==7.4.2
reportbug==12.0.0
requests==2.32.5
rfc3987==1.3.8
rsa==4.9.1
scipy==1.10.1
SCons==4.4.0
sexpdata==1.0.0
six==1.16.0
soupsieve==2.3.2
sympy==1.11.1
tabulate==0.8.10
texttable==1.6.7
tld==0.13.1
tsplib95==0.7.1
typing_extensions==4.4.0
ufoLib2==0.14.0
unidiff==0.7.5
uritemplate==4.1.1
urllib3==1.26.12
wadllib==1.3.6
webcolors==1.11.1
webencodings==0.5.1
websocket-client==1.2.3
Werkzeug==3.1.3
wrapt==1.17.3
xdg==5
xdot==1.2
#+end_example

* Default script
#+begin_src shell :results output :exports both
ssh pcad
ssh poti
cd $SCRATCH
. ./venv/bin/activate

#+end_src
* Synchronous parallel training
:PROPERTIES:
:header-args: :tangle train-sync.py :tangle-mode (identity #o755)
:END:
** Libraries
Needed libraries

#+begin_src python :session *P* :results output :exports both
import tensorflow as tf
from tensorflow.keras.utils import to_categorical
from tensorflow.keras.applications import ResNet50
from tensorflow.keras.applications import EfficientNetB0
from tensorflow.keras.models import Model
from tensorflow.keras.layers import Dense, GlobalAveragePooling2D
from tensorflow.keras.optimizers import SGD
import os
import json
import time
from datetime import datetime
import subprocess
import pathlib
import pandas as pd
import csv
import argparse
#+end_src
** Inputs for the training

#+begin_src python :session *P* :results output :exports both
parser = argparse.ArgumentParser(description='Configure the training.')
parser.add_argument('-m', '--model', choices=['ResNet50', 'EfficientNetB0'], required=True, help='Model to be trained.')
parser.add_argument('-b', '--batch', type=int, required=True, help='Batch size for train.')

args = parser.parse_args()
#+end_src


To run the training you can run with:

=python train-sync.py -m ResNet50 -b 256=

This will run the ResNet50 with batchsize 256


** TensorFlow Callbacks

#+begin_src python :session *P* :results output :exports both
log_dir = "logs/profile/" + datetime.now().strftime("%Y%m%d-%H%M%S")

tensorboard_callback = tf.keras.callbacks.TensorBoard(
    log_dir=log_dir,
    histogram_freq=1,
    profile_batch='2,10'  # Profile batches 100 through 105
)
#+end_src

** Hyper parameters and configurations (cluster and training)

Here are the run configurations but also the " tf config" configurations
#+begin_src python :session *P* :results output :exports both
SEED = 1
IMG_SIZE = (224, 224)
EPOCHS = 5
BATCH_SIZE_PER_REPLICA = args.batch
NUM_CLASSES = 200
NUM_TRAIN_IMAGES = 100000
epoch_start_time = 0.0 #var global

tf.random.set_seed(SEED)

strategy = tf.distribute.MultiWorkerMirroredStrategy()
tf_config_str = os.environ.get('TF_CONFIG', '{}')
tf_config = json.loads(tf_config_str)
task_info = tf_config.get('task', {})
worker_id = task_info.get('index', 0)

NUM_WORKERS = strategy.num_replicas_in_sync
GLOBAL_BATCH_SIZE = BATCH_SIZE_PER_REPLICA * NUM_WORKERS

print(f"Worker ID: {worker_id}")
print(f"Número total de workers (réplicas): {NUM_WORKERS}")
print(f"Batch size por worker: {BATCH_SIZE_PER_REPLICA}")
print(f"Batch size global (total): {GLOBAL_BATCH_SIZE}\n")


def start_timer(epoca, logs):
    global epoch_start_time
    epoch_start_time = time.time()

#+end_src

** Timer functions
#+begin_src python :session *P* :results output :exports both
def end_timer(epoca, logs):

    global epoch_start_time
    tempo = time.time() - epoch_start_time

    if worker_id == 0:
        with open(csv_file, mode='a', newline='') as file:
            csv.writer(file).writerow([epoca,tempo])


#+end_src

** TODO Dataset load and config
Here is where the dataset is loaded, its needed to optimize this to
the parallel environment.

https://www.tensorflow.org/guide/data_performance

*** Dataset Loading Function
#+begin_src python :session *P* :results output :exports both
def load_tiny_imagenet_datasets(data_path, num_classes):
    train_path = data_path / 'train'
    val_path = data_path / 'val'

    train_dataset = tf.keras.utils.image_dataset_from_directory(
        train_path,
        labels='inferred',
        label_mode='categorical',
        image_size=(64, 64),
        interpolation='nearest',
        batch_size=None,
        shuffle=False
    )

    val_annotations_path = val_path / 'val_annotations.txt'
    val_data = pd.read_csv(val_annotations_path, sep='\t', header=None, names=['File', 'Class', 'X1', 'Y1', 'X2', 'Y2'])

    class_names = sorted(os.listdir(train_path))
    class_to_idx = {name: index for index, name in enumerate(class_names)}

    val_images = [str(val_path / 'images' / fname) for fname in val_data['File']]
    val_labels_str = val_data['Class'].values
    val_labels_int = [class_to_idx[name] for name in val_labels_str]
    val_labels_cat = to_categorical(val_labels_int, num_classes=num_classes)

    val_dataset = tf.data.Dataset.from_tensor_slices((val_images, val_labels_cat))

    def parse_image(filename, label):
        image = tf.io.read_file(filename)
        image = tf.image.decode_jpeg(image, channels=3)
        return image, label

    val_dataset = val_dataset.map(parse_image, num_parallel_calls=tf.data.AUTOTUNE)

    return train_dataset, val_dataset

#+end_src

*** Preparing data for training
#+begin_src python :session *P* :results output :exports both

print("Carregando o dataset Tiny ImageNet de um diretório local...")
tiny_imagenet_path = pathlib.Path('/scratch/rrdmatos/tiny-imagenet-200') # Set here the path to the dataset
train_dataset, test_dataset = load_tiny_imagenet_datasets(tiny_imagenet_path, NUM_CLASSES)

def preprocess_image(image, label):
    image = tf.cast(image, tf.float32) / 255.0
    image = tf.image.resize(image, IMG_SIZE)
    return image, label

# options = tf.data.Options()
# options.experimental_distribute.auto_shard_policy = tf.data.experimental.AutoShardPolicy.DATA

train_dataset = train_dataset.batch(GLOBAL_BATCH_SIZE).map(preprocess_image, num_parallel_calls=tf.data.AUTOTUNE).cache().prefetch(tf.data.AUTOTUNE)

test_dataset = test_dataset.batch(GLOBAL_BATCH_SIZE).map(preprocess_image, num_parallel_calls=tf.data.AUTOTUNE).cache().prefetch(tf.data.AUTOTUNE)#test_dataset.batch(GLOBAL_BATCH_SIZE).map(preprocess_image, num_parallel_calls=tf.data.AUTOTUNE).cache().prefetch(tf.data.AUTOTUNE)
#+end_src

** Build and compile the model
Using the strategy to create compile the model
#+begin_src python :session *P* :results output :exports both
with strategy.scope():
    input_shape = (224, 224, 3)

    if args.model == 'ResNet50':
        base_model = ResNet50(weights=None, include_top=False, input_shape=(224,224,3))
    elif args.model == 'EfficientNetB0':
        base_model = EfficientNetB0(weights=None, include_top=False, input_shape=(224,224,3))


    base_model.trainable = True

    x = GlobalAveragePooling2D()(base_model.output)
    x = Dense(1024, activation="relu")(x)
    predictions = Dense(NUM_CLASSES, activation="softmax")(x)

    model = Model(inputs=base_model.input, outputs=predictions)

    BASE_LEARNING_RATE = 0.01
    SCALED_LEARNING_RATE = BASE_LEARNING_RATE * NUM_WORKERS

    opt = SGD(learning_rate=SCALED_LEARNING_RATE, weight_decay=0.0001, momentum=0.9)

    model.compile(optimizer=opt, loss="categorical_crossentropy", metrics=["accuracy"])
#+end_src

** CSV Callback
#+begin_src python :session *P* :results output :exports both
timestamp = datetime.now().strftime("%d_%H%M%S")
csv_file = f"logs_sync/log_{args.model}_{timestamp}.csv"
if worker_id == 0:
    os.makedirs(os.path.dirname(csv_file), exist_ok=True)
    with open(csv_file, mode='w', newline='') as file:
        csv.writer(file).writerow(['epoca', 'tempo'])

csv_callback = tf.keras.callbacks.LambdaCallback(on_epoch_begin=start_timer,on_epoch_end=end_timer)
#+end_src
** Actual training

Here is where the training really happens, we are using the model.fit
function.
#+begin_src python :session *P* :results output :exports both
duration_seconds = 0
print("Iniciando o treinamento distribuído...")

start_train_time = time.perf_counter()
history = model.fit(train_dataset, epochs=EPOCHS, validation_data=test_dataset,  callbacks=[tensorboard_callback,csv_callback])
close_train_time = time.perf_counter()

duration_seconds = close_train_time - start_train_time

score = model.evaluate(test_dataset, verbose=(1 if worker_id == 0 else 0))
if worker_id == 0:
    print("\nIniciando avaliação final no worker 0...")
    print(f"Loss (perda) no teste: {score[0]:.4f}")
    print(f"Accuracy (acurácia) no teste: {score[1]:.4f}")

print("TIME TRAINING:", duration_seconds)
print(f"\nWorker {worker_id} concluiu.")
#+end_src


* Notes on the training

Values for the first epoch

|              | Time    |
|--------------+---------|
| Prefetch     | ~ 04:50 |
| No Prefetch  | ~ 05:00 |

Prefetch will stay.

|                 | Time    |
|-----------------+---------|
| Map parallel    | ~ 05:00 |
| No Map Parallel | ~ 04:50 |

* Experimental design

If i use 128 as batch size in the Tupi machine, i get a OOM error, so
we will set the maximum batch size as 1000
#+begin_src R :results output :session *R* :exports both :noweb yes :colnames yes
options(crayon.enabled=FALSE)
#install.packages('DoE.base', dependencies = TRUE, repos='http://cran.rstudio.com/')
library(DoE.base)
library(tidyverse)

fator_modelo = c("ResNet50", "EfficientNetB0")
fator_batch = c(32, 64, 100)
fator_tipo = c("sync", "ctl")
fator_nodos = c(1,2,3,4)

fac.design(nfactors=4,
           replications=5,
           repeat.only= FALSE ,
           randomize=TRUE,
           seed=14,
           nlevels=c(length(fator_nodos),
                     length(fator_tipo),
                     length(fator_modelo),
                     length(fator_batch)),
           factor.names=list(
             nodos = fator_nodos,
             tipo = fator_tipo,
             modelo = fator_modelo,
             batch = fator_batch
           )) |>
  as_tibble() |>
  mutate_at(vars(nodos:modelo), as.character) |>
  #filter(threads == 16) |>
  print() |>
  write_csv("projeto_experimental.csv", progress=FALSE)
  read_csv("projeto_experimental.csv", progress=FALSE, show_col_types=FALSE) |>
  print(n=100)

#+end_src

#+RESULTS:
#+begin_example
creating full factorial with 48 runs ...

# A tibble: 240 × 5
   nodos tipo  modelo         batch Blocks
   <chr> <chr> <chr>          <fct> <fct>
 1 1     sync  EfficientNetB0 32    .1
 2 4     ctl   EfficientNetB0 100   .1
 3 3     sync  EfficientNetB0 32    .1
 4 3     sync  EfficientNetB0 100   .1
 5 2     ctl   EfficientNetB0 32    .1
 6 4     ctl   EfficientNetB0 32    .1
 7 2     sync  EfficientNetB0 64    .1
 8 1     sync  ResNet50       100   .1
 9 3     sync  ResNet50       32    .1
10 4     sync  EfficientNetB0 64    .1
# ℹ 230 more rows
# ℹ Use `print(n = ...)` to see more rows
# A tibble: 240 × 5
    nodos tipo  modelo         batch Blocks
    <dbl> <chr> <chr>          <dbl>  <dbl>
  1     1 sync  EfficientNetB0    32    0.1
  2     4 ctl   EfficientNetB0   100    0.1
  3     3 sync  EfficientNetB0    32    0.1
  4     3 sync  EfficientNetB0   100    0.1
  5     2 ctl   EfficientNetB0    32    0.1
  6     4 ctl   EfficientNetB0    32    0.1
  7     2 sync  EfficientNetB0    64    0.1
  8     1 sync  ResNet50         100    0.1
  9     3 sync  ResNet50          32    0.1
 10     4 sync  EfficientNetB0    64    0.1
 11     1 ctl   ResNet50         100    0.1
 12     3 sync  ResNet50         100    0.1
 13     1 sync  ResNet50          32    0.1
 14     3 ctl   ResNet50          64    0.1
 15     1 sync  EfficientNetB0   100    0.1
 16     1 ctl   ResNet50          32    0.1
 17     2 sync  EfficientNetB0   100    0.1
 18     1 ctl   EfficientNetB0   100    0.1
 19     1 sync  EfficientNetB0    64    0.1
 20     4 sync  ResNet50          64    0.1
 21     4 ctl   ResNet50          32    0.1
 22     4 sync  ResNet50         100    0.1
 23     4 sync  EfficientNetB0   100    0.1
 24     2 ctl   ResNet50          32    0.1
 25     4 sync  EfficientNetB0    32    0.1
 26     3 ctl   EfficientNetB0    64    0.1
 27     2 sync  ResNet50         100    0.1
 28     2 ctl   EfficientNetB0    64    0.1
 29     3 ctl   ResNet50         100    0.1
 30     4 sync  ResNet50          32    0.1
 31     2 ctl   ResNet50          64    0.1
 32     1 sync  ResNet50          64    0.1
 33     3 sync  EfficientNetB0    64    0.1
 34     3 ctl   EfficientNetB0   100    0.1
 35     3 ctl   EfficientNetB0    32    0.1
 36     2 sync  EfficientNetB0    32    0.1
 37     1 ctl   ResNet50          64    0.1
 38     2 sync  ResNet50          32    0.1
 39     1 ctl   EfficientNetB0    64    0.1
 40     4 ctl   EfficientNetB0    64    0.1
 41     4 ctl   ResNet50         100    0.1
 42     2 sync  ResNet50          64    0.1
 43     3 ctl   ResNet50          32    0.1
 44     2 ctl   EfficientNetB0   100    0.1
 45     1 ctl   EfficientNetB0    32    0.1
 46     2 ctl   ResNet50         100    0.1
 47     4 ctl   ResNet50          64    0.1
 48     3 sync  ResNet50          64    0.1
 49     4 ctl   EfficientNetB0    32    0.2
 50     2 sync  EfficientNetB0    64    0.2
 51     2 ctl   EfficientNetB0   100    0.2
 52     3 ctl   EfficientNetB0   100    0.2
 53     3 ctl   ResNet50          64    0.2
 54     3 ctl   EfficientNetB0    64    0.2
 55     2 ctl   ResNet50         100    0.2
 56     3 sync  EfficientNetB0    32    0.2
 57     1 sync  ResNet50         100    0.2
 58     1 sync  EfficientNetB0   100    0.2
 59     3 ctl   EfficientNetB0    32    0.2
 60     2 sync  EfficientNetB0   100    0.2
 61     2 ctl   EfficientNetB0    64    0.2
 62     4 ctl   EfficientNetB0    64    0.2
 63     1 ctl   ResNet50          64    0.2
 64     2 ctl   ResNet50          64    0.2
 65     1 ctl   ResNet50          32    0.2
 66     2 sync  ResNet50          32    0.2
 67     4 sync  EfficientNetB0    32    0.2
 68     2 sync  ResNet50         100    0.2
 69     1 ctl   ResNet50         100    0.2
 70     2 ctl   ResNet50          32    0.2
 71     4 ctl   ResNet50          64    0.2
 72     2 sync  EfficientNetB0    32    0.2
 73     3 ctl   ResNet50         100    0.2
 74     3 sync  EfficientNetB0   100    0.2
 75     4 ctl   ResNet50          32    0.2
 76     4 sync  ResNet50          64    0.2
 77     4 ctl   ResNet50         100    0.2
 78     1 sync  EfficientNetB0    32    0.2
 79     4 sync  ResNet50          32    0.2
 80     4 sync  ResNet50         100    0.2
 81     3 ctl   ResNet50          32    0.2
 82     1 ctl   EfficientNetB0    32    0.2
 83     3 sync  ResNet50          64    0.2
 84     4 sync  EfficientNetB0    64    0.2
 85     4 ctl   EfficientNetB0   100    0.2
 86     1 sync  EfficientNetB0    64    0.2
 87     2 ctl   EfficientNetB0    32    0.2
 88     3 sync  ResNet50         100    0.2
 89     1 ctl   EfficientNetB0    64    0.2
 90     1 sync  ResNet50          32    0.2
 91     1 sync  ResNet50          64    0.2
 92     2 sync  ResNet50          64    0.2
 93     4 sync  EfficientNetB0   100    0.2
 94     3 sync  EfficientNetB0    64    0.2
 95     1 ctl   EfficientNetB0   100    0.2
 96     3 sync  ResNet50          32    0.2
 97     4 ctl   EfficientNetB0    32    0.3
 98     2 sync  EfficientNetB0   100    0.3
 99     3 ctl   EfficientNetB0    32    0.3
100     2 ctl   ResNet50          32    0.3
# ℹ 140 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example
