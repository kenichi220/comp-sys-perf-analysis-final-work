#+STARTUP: content
#+STARTUP: overview
#+STARTUP: indent
#+TITLE: TensorFlow Results
#+AUTHOR: Rayan Raddatz de Matos, Marcelo Gulart, Kenichi Brumati

* Parcial
** Load data

Data is in the data directory:
#+begin_src shell :session *shell* :results output :exports both
ls --color=never ..
pwd ..
#+end_src

#+RESULTS:
: ai-env	KB-notes.org  meetings.org  presentations  README.org  RR-notes.org
: data	manifest.scm  paper	    Pytorch	   references  tensorflow
: /home/rayan/graduation/estudos/perf/final/tensorflow

#+begin_src R :results output :session *R* :exports both
options(crayon.enabled=FALSE)
suppressMessages(library(fs))
suppressMessages(library(tidyverse))
suppressMessages(library(janitor))
suppressMessages(library(dplyr))
suppressMessages(library(lubridate))

BASE="./data"
tibble(CSV = dir_ls(BASE, regexp = "csv$", recurse=TRUE)) |>
  filter(!str_detect(CSV, ":")) |>
  filter(!str_detect(CSV, "ep")) |>
  mutate(DATA = map(CSV, read_csv, progress=FALSE, show_col_types = FALSE)) |>
  separate(CSV, into=paste0("XX", 1:5), sep="/")  |>
  separate(XX5, into=paste0("YY", 1:2), sep="_") |>
  mutate(type=XX4) |> #|>
  mutate(machine = gsub("\\.csv", "", YY2)) |>
  mutate(machine = gsub("\\-.*", "", machine)) |>
  rowid_to_column("id") |>
  select(id, type, machine, DATA) |>
  unnest(DATA) |>  clean_names() |>
  mutate(
    timestamp_fixed = str_replace(timestamp, "(\\d{4}/\\d{2}/\\d{2})", "\\1 "),
    timestamp = ymd_hms(timestamp_fixed)
  ) |>
  group_by(id, type, machine) |>
  mutate(
    elapsed_seconds = as.numeric(timestamp - min(timestamp))
  ) |>
  ungroup() |> select(elapsed_seconds, everything()) -> df

## df
names(df)
df |> select(type) |> distinct()
#+end_src

#+RESULTS:
#+begin_example
 [1] "elapsed_seconds"         "id"                      "type"
 [4] "machine"                 "timestamp"               "index"
 [7] "name"                    "driver_version"          "utilization_gpu"
[10] "memory_total"            "memory_used"             "power_draw"
[13] "temperature_gpu"         "clocks_current_graphics" "clocks_current_sm"
[16] "clocks_current_memory"   "timestamp_fixed"
# A tibble: 3 × 1
  type
  <chr>
1 node
2 nodes2
3 nodes3
#+end_example

#+begin_src R :results output :session *R* :exports both
names(df)
df |> select(power_draw)
#+end_src

#+RESULTS:
#+begin_example
 [1] "timestamp"               "index"                   "name"
 [4] "driver_version"          "utilization_gpu"         "memory_total"
 [7] "memory_used"             "power_draw"              "temperature_gpu"
[10] "clocks_current_graphics" "clocks_current_sm"       "clocks_current_memory"
[13] "timestamp_fixed"         "machine"
# A tibble: 57,260 × 1
   power_draw
        <dbl>
 1       54.1
 2       54.1
 3       54.1
 4       54.2
 5       54.2
 6       54.2
 7       54.2
 8       54.2
 9       54.2
10       54.2
# ℹ 57,250 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

** Graphs style
#+begin_src R :results output :session *R* :exports both

meu_estilo <- function() {
  list(
    theme_bw(base_size = 20),
    theme(
      legend.title = element_blank(),
      plot.margin = unit(c(0, 0, 0, 0), "cm"),
      legend.spacing = unit(1, "mm"),
      legend.position = "right",
      legend.justification = "left",
      legend.box.spacing = unit(0, "pt"),
      legend.box.margin = margin(0, 0, 0, 0),
      axis.text.x = element_text(angle=45, vjust=1, hjust=1)
    ))
}
#+end_src

#+RESULTS:

** GPU usage
*** ONE NODE: GPU usage

#+begin_src R :results output graphics file :file (org-babel-temp-file "figure" ".png") :exports both :width 600 :height 400 :session *R*
df |> filter(type=="node") |> group_split(id) |> first() |>
  ggplot(aes(x = elapsed_seconds, y = utilization_gpu)) +
  # Only include the geom_line() layer for a line chart
  geom_line(color = "black", linewidth = 1) +
  labs(
    title = "GPU Utilization (1 Node)",
    x = "TimeStamp",
    y = "GPU Utilization (%)"
  ) + meu_estilo() +
  facet_wrap(~ machine)

ggsave(
  filename = "time-one-machine.png",
  device = png,
  width=20,
  heigh=10
)
#+end_src

#+RESULTS:
[[file:/tmp/babel-P5m5UD/figureTAvIx5.png]]

*** TWO NODES: GPU usage

#+begin_src R :results output graphics file :file (org-babel-temp-file "figure" ".png") :exports both :width 600 :height 400 :session *R*
df |> filter(type=="nodes2") |>
  ggplot(aes(x = elapsed_seconds, y = utilization_gpu)) +
  # Only include the geom_line() layer for a line chart
  geom_line(color = "black", linewidth = 0.1) +
  labs(
    title = "GPU Utilization (2 Nodes)",
    x = "TimeStamp",
    y = "GPU Utilization (%)"
  ) + meu_estilo() +
  facet_grid(. ~ machine)

ggsave(
  filename = "time-two-machine.png",
  device = png,
  width=20,
  heigh=10
)
#+end_src

#+RESULTS:
[[file:/tmp/babel-P5m5UD/figurerUgDFK.png]]

*** THREE NODES: GPU usage

#+begin_src R :results output graphics file :file (org-babel-temp-file "figure" ".png") :exports both :width 600 :height 400 :session *R*
df |> filter(type=="nodes3") |>
  ggplot(aes(x = elapsed_seconds, y = utilization_gpu)) +
  # Only include the geom_line() layer for a line chart
  geom_line(color = "black", linewidth = 0.1) +
  labs(
    title = "GPU Utilization (3 Nodes)",
    x = "TimeStamp",
    y = "GPU Utilization (%)"
  ) + meu_estilo() +
  facet_grid(. ~ machine)

ggsave(
  filename = "time-three-machine.png",
  device = png,
  width=20,
  heigh=10
)
#+end_src

#+RESULTS:
[[file:/tmp/babel-P5m5UD/figure4CRaYi.png]]

**** One machine of the train of 3 nodes

#+begin_src R :results output graphics file :file (org-babel-temp-file "figure" ".png") :exports both :width 600 :height 400 :session *R*
df |> filter(type=="nodes3")  |> group_split(id) |> first() |>
  ggplot(aes(x = elapsed_seconds, y = utilization_gpu)) +
  # Only include the geom_line() layer for a line chart
  geom_line(color = "black", linewidth = 0.1) +
  labs(
    title = "GPU Utilization (3 Nodes)",
    x = "TimeStamp",
    y = "GPU Utilization (%)"
  ) + meu_estilo() +
  facet_grid(. ~ machine)

ggsave(
  filename = "time-three-machine-one-machine.pdf",
  device = pdf,
  width=20,
  heigh=10
)
#+end_src

#+RESULTS:
[[file:/tmp/babel-P5m5UD/figureKDRUiz.png]]

*** GPU USAGE FOR RANGE
#+begin_src R :results output graphics file :file  (org-babel-temp-file "figure" ".png") :exports both :width 600 :height 400 :session *R*

start_time <- 6480
end_time   <- 6540

df |> filter(type=="nodes3") |> group_split(id) |> first() |>
  ggplot(aes(x = elapsed_seconds, y = utilization_gpu)) +
  geom_line(color = "black", linewidth = 1) +
  labs(
    title = "One minute around the penultimate epoch (3 Nodes)",
    x = "Time",
    y = "GPU Utilization (%)"
  ) +
  meu_estilo() +
   coord_cartesian(
    xlim = c(start_time, end_time)
  ) + facet_wrap(~ machine)

ggsave(
  filename = "time-three-node.png",
  device = png,
  width=20,
  heigh=10
)
#+end_src

#+RESULTS:
[[file:/tmp/babel-P5m5UD/figureCV30Im.png]]

** Energy
*** Energy
#+begin_src R :results output graphics file :file (org-babel-temp-file "figure" ".png") :exports both :width 1200 :height 800 :session *R*

start_time <- 1130
end_time   <- 1190

df |> filter(type=="nodes3") |> group_split(id) |> first() |>
  mutate(
    # Calculate time difference in seconds since the last measurement
    time_diff_secs = as.numeric(timestamp - lag(timestamp)),
    # Calculate average power in the interval for better accuracy
    avg_power_w = (power_draw + lag(power_draw)) / 2,
    # Calculate energy for the interval (Joules = Watts * seconds)
    energy = avg_power_w * time_diff_secs
  ) |>
  filter(!is.na(energy)) |>
#  group_split(id) |> first() |>
  ggplot(aes(x = elapsed_seconds, y = energy)) +
    geom_line(color = "dodgerblue", linewidth = 0.5) +
  labs(
    title = "GPU energy usage on one minute around the penultimate epoch (2 Node)",
    x = "Time",
    y = "Energy"
  ) +
  meu_estilo() + facet_wrap(~ machine) +
     coord_cartesian(
    xlim = c(start_time, end_time)
  ) +
  scale_y_continuous(limits = c(0, NA)) # Ensure y-axis starts at 0

ggsave(
  filename = "energy-two-machine-range.png",
  device = png
)
#+end_src

#+RESULTS:
[[file:/tmp/babel-P5m5UD/figureh5YWtk.png]]
** Temperature
*** Temp
#+begin_src R :results output graphics file :file (org-babel-temp-file "figure" ".png") :exports both :width 1200 :height 800 :session *R*

start_time <- 1130
end_time   <- 1190

df |> filter(type=="nodes3") |> #group_split(id) |> first() |>

  ggplot(aes(x = elapsed_seconds, y = temperature_gpu)) +
    geom_line(color = "red", linewidth = 0.5) +
  labs(
    title = "GPU energy usage on one minute around the penultimate epoch (2 Node)",
    x = "Time",
    y = "Energy"
  ) +
  meu_estilo() + facet_wrap(~ machine) +
     coord_cartesian(
    xlim = c(start_time, end_time)
  ) +
  scale_y_continuous(limits = c(0, NA)) # Ensure y-axis starts at 0

ggsave(
  filename = "energy-two-machine-range.png",
  device = png
)
#+end_src

#+RESULTS:
[[file:/tmp/babel-P5m5UD/figurez1CFuj.png]]

** Epoch info
*** Load epochs data
#+begin_src R :results output :session *R* :exports both
options(crayon.enabled=FALSE)
suppressMessages(library(fs))
suppressMessages(library(tidyverse))
suppressMessages(library(janitor))
suppressMessages(library(dplyr))
suppressMessages(library(lubridate))

BASE="./data"
tibble(CSV = dir_ls(BASE, regexp = "csv$", recurse=TRUE)) |>
  filter(str_detect(CSV, "epoch")) |>
  mutate(DATA = map(CSV, read_csv, progress=FALSE, show_col_types = FALSE)) |>
  separate(CSV, into=paste0("XX", 1:5), sep="/") |>
  separate(XX5, into=paste0("YY", 1:2), sep="-") |>
  mutate(nodes = gsub("\\.csv", "", YY2)) |>
  select(nodes, DATA) |>
  unnest(DATA) |> clean_names() |>
  group_by(nodes) |> mutate(cumulative_time = cumsum(time)) |>
  ungroup() -> epochs

names(epochs)
epochs
#+end_src

#+RESULTS:
#+begin_example
[1] "nodes"           "epoch"           "time"            "loss"            "accuracy"
[6] "cumulative_time"
# A tibble: 15 × 6
   nodes epoch  time  loss accuracy cumulative_time
   <chr> <dbl> <dbl> <dbl>    <dbl>           <dbl>
 1 one       1   130 1.70     0.390             130
 2 one       2    89 1.50     0.494             219
 3 one       3    89 1.75     0.518             308
 4 one       4    89 1.11     0.631             397
 5 one       5    89 1.06     0.650             486
 6 two       1   324 2.00     0.277             324
 7 two       2   277 1.39     0.489             601
 8 two       3   277 1.12     0.592             878
 9 two       4   277 0.914    0.671            1155
10 two       5   279 0.75     0.733            1434
11 three     1  1666 2.17     0.238            1666
12 three     2  1618 1.54     0.417            3284
13 three     3  1620 1.30     0.518            4904
14 three     4  1619 1.09     0.603            6523
15 three     5  1620 0.923    0.668            8143
#+end_example

*** Quering per epoch info
#+begin_src R :results output graphics file :file (org-babel-temp-file "figure" ".png") :exports both :width 1200 :height 800 :session *R*
epochs |>
  ggplot(aes(x = epoch, y = loss, color=nodes, group=nodes)) +
  geom_point(size = 5) +
  geom_line(linewidth = 2) +
  labs(
    title = "Training accuracy",
    x = "Epoch",
    y = "Accuracy"
  ) +
  meu_estilo() #+ #facet_wrap(~ nodes) +

## ggsave(
##   filename = "accuracy-for-epoch.png",
##   device = png
## )
#+end_src

#+RESULTS:
[[file:/tmp/babel-P5m5UD/figuregVUIOd.png]]


* Final
Data is in the data directory:
#+begin_src shell :session *shell* :results output :exports both
ls --color=never ..
pwd ..
#+end_src

#+RESULTS:
:
: data  manifest.scm  paper	   README.org  RR-notes.org  timelogs
: imgs  meetings.org  presentations  references  tensorflow
: /home/rayan/graduation/estudos/perf/final/tensorflow

** Load combinations and run ID
#+begin_src R :results output :session *R* :exports both
options(crayon.enabled=FALSE)
suppressMessages(library(fs))
suppressMessages(library(tidyverse))
suppressMessages(library(janitor))
suppressMessages(library(dplyr))
suppressMessages(library(lubridate))

BASE="data/final/"
tibble(PATH = dir_ls(BASE, regexp = "out$", recurse=TRUE))  |>
  ## filter(!str_detect(CSV, ":")) |>
  ## filter(!str_detect(CSV, "ep")) |>
  ## mutate(DATA = map(CSV, read_csv, progress=FALSE, show_col_types = FALSE)) |>
  separate(PATH, into=paste0("XX", 1:3), sep="/") |>
  separate(XX3, into=paste0("YY", 1:6), sep="_") |>
  ## mutate(type=XX4) |> #|>
  mutate(YY6 = gsub("\\.out", "", YY6)) |>
  ## mutate(machine = gsub("\\-.*", "", machine)) |>
  ## rowid_to_column("id") |>
  select(id=YY6, model=YY2, batch=YY4, type=YY3, nodes=YY5) -> configs

configs
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 198 × 5
   id     model          batch type  nodes
   <chr>  <chr>          <chr> <chr> <chr>
 1 735831 EfficientNetB0 100   ctl   1
 2 735908 EfficientNetB0 100   ctl   1
 3 735920 EfficientNetB0 100   ctl   1
 4 735970 EfficientNetB0 100   ctl   1
 5 736028 EfficientNetB0 100   ctl   1
 6 736718 EfficientNetB0 100   ctl   1
 7 736719 EfficientNetB0 100   ctl   1
 8 736720 EfficientNetB0 100   ctl   1
 9 736721 EfficientNetB0 100   ctl   1
10 736727 EfficientNetB0 100   ctl   1
# ℹ 188 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

** Get the data and join with the run
#+begin_src R :results output :session *R* :exports both
BASE="data/final/logs/"
tibble(CSV = dir_ls(BASE, regexp = "csv$", recurse=TRUE)) |>
  ## filter(!str_detect(CSV, ":")) |>
  ## filter(!str_detect(CSV, "ep")) |>
  mutate(DATA = map(CSV, read_csv, progress=FALSE, show_col_types = FALSE)) |>
  separate(CSV, into=paste0("XX", 1:7), sep="/") |>
  separate(XX4, into=paste0("YY", 1:2), sep="_")  |>
  select(id=YY2, DATA) |>
  mutate(
    DATA = map(DATA, function(x) {
      if (is.data.frame(x)) {
        return(x %>% mutate(epoca = as.character(epoca),
                            tempo = as.character(tempo)))
      } else {
        return(x)
      }
    })
  ) |>
  unnest(DATA) -> results

results
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 724 × 3
   id     epoca tempo
   <chr>  <chr> <chr>
 1 735818 0     298.360699892044
 2 735818 1     275.497747659683
 3 735818 2     274.337224721909
 4 735818 3     274.618661165237
 5 735818 4     273.248296737671
 6 735824 0     337.033883571625
 7 735824 1     317.953343868256
 8 735824 2     318.067031621933
 9 735824 3     317.879886865616
10 735824 4     317.93927025795
# ℹ 714 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

** Join the data

#+begin_src R :results output :session *R* :exports both
df <- configs |>
  left_join(results, by="id")

df
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 757 × 7
   id     model          batch type  nodes epoca tempo
   <chr>  <chr>          <chr> <chr> <chr> <chr> <chr>
 1 735831 EfficientNetB0 100   ctl   1     <NA>  <NA>
 2 735908 EfficientNetB0 100   ctl   1     0     350.095215320587
 3 735920 EfficientNetB0 100   ctl   1     0     344.294260263443
 4 735920 EfficientNetB0 100   ctl   1     1     333.430016517639
 5 735970 EfficientNetB0 100   ctl   1     0     344.588366985321
 6 736028 EfficientNetB0 100   ctl   1     <NA>  <NA>
 7 736718 EfficientNetB0 100   ctl   1     0     344.885010719299
 8 736719 EfficientNetB0 100   ctl   1     0     345.933742523193
 9 736719 EfficientNetB0 100   ctl   1     1     334.544350147247
10 736720 EfficientNetB0 100   ctl   1     0     344.251515865326
# ℹ 747 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

** Runs with error
#+begin_src R :results output :session *R* :exports both
df |> group_by(nodes, type, model, batch, id) |>
  summarize(N=n(), .groups="keep") |>
  arrange(N) |>
  filter(N < 5 | N > 5) |> print() |>
  select(-N) -> errors

errors |> print(n=100)

do.call("rbind", replicate(5, errors, simplify = FALSE)) |>
  write_csv("errors_rerun.csv", progress=FALSE)
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 62 × 6
# Groups:   nodes, type, model, batch, id [62]
   nodes type  model          batch id         N
   <chr> <chr> <chr>          <chr> <chr>  <int>
 1 1     ctl   EfficientNetB0 100   735831     1
 2 1     ctl   EfficientNetB0 100   735908     1
 3 1     ctl   EfficientNetB0 100   735970     1
 4 1     ctl   EfficientNetB0 100   736028     1
 5 1     ctl   EfficientNetB0 100   736718     1
 6 1     ctl   EfficientNetB0 100   736721     1
 7 1     ctl   EfficientNetB0 100   736727     1
 8 1     ctl   EfficientNetB0 100   736728     1
 9 1     ctl   EfficientNetB0 100   736729     1
10 1     ctl   EfficientNetB0 100   736730     1
# ℹ 52 more rows
# ℹ Use `print(n = ...)` to see more rows
# A tibble: 62 × 5
# Groups:   nodes, type, model, batch, id [62]
   nodes type  model          batch id
   <chr> <chr> <chr>          <chr> <chr>
 1 1     ctl   EfficientNetB0 100   735831
 2 1     ctl   EfficientNetB0 100   735908
 3 1     ctl   EfficientNetB0 100   735970
 4 1     ctl   EfficientNetB0 100   736028
 5 1     ctl   EfficientNetB0 100   736718
 6 1     ctl   EfficientNetB0 100   736721
 7 1     ctl   EfficientNetB0 100   736727
 8 1     ctl   EfficientNetB0 100   736728
 9 1     ctl   EfficientNetB0 100   736729
10 1     ctl   EfficientNetB0 100   736730
11 1     ctl   EfficientNetB0 100   736731
12 1     ctl   EfficientNetB0 100   736737
13 1     ctl   EfficientNetB0 100   736738
14 1     ctl   EfficientNetB0 100   736739
15 1     ctl   EfficientNetB0 100   736740
16 1     ctl   EfficientNetB0 100   736741
17 1     ctl   EfficientNetB0 100   736747
18 1     ctl   EfficientNetB0 100   736748
19 1     ctl   EfficientNetB0 100   736749
20 1     ctl   EfficientNetB0 100   736751
21 1     ctl   EfficientNetB0 100   736757
22 1     ctl   EfficientNetB0 100   736760
23 1     ctl   EfficientNetB0 100   736767
24 2     ctl   EfficientNetB0 100   735857
25 2     ctl   EfficientNetB0 100   735864
26 2     ctl   EfficientNetB0 100   735916
27 2     ctl   EfficientNetB0 100   735969
28 2     ctl   EfficientNetB0 100   736013
29 2     ctl   EfficientNetB0 100   736722
30 2     ctl   EfficientNetB0 100   736723
31 2     ctl   EfficientNetB0 100   736724
32 2     ctl   EfficientNetB0 100   736725
33 2     ctl   EfficientNetB0 100   736726
34 2     ctl   EfficientNetB0 100   736732
35 2     ctl   EfficientNetB0 100   736734
36 2     ctl   EfficientNetB0 100   736735
37 2     ctl   EfficientNetB0 100   736736
38 2     ctl   EfficientNetB0 100   736742
39 2     ctl   EfficientNetB0 100   736743
40 2     ctl   EfficientNetB0 100   736745
41 2     ctl   EfficientNetB0 100   736746
42 2     ctl   EfficientNetB0 100   736752
43 2     ctl   EfficientNetB0 100   736753
44 2     ctl   EfficientNetB0 100   736754
45 2     ctl   EfficientNetB0 100   736755
46 2     ctl   EfficientNetB0 100   736762
47 2     ctl   EfficientNetB0 100   736763
48 2     ctl   EfficientNetB0 100   736764
49 2     ctl   EfficientNetB0 100   736766
50 3     ctl   EfficientNetB0 100   735847
51 3     ctl   EfficientNetB0 100   735865
52 1     ctl   EfficientNetB0 100   735920
53 1     ctl   EfficientNetB0 100   736719
54 1     ctl   EfficientNetB0 100   736720
55 1     ctl   EfficientNetB0 100   736750
56 1     ctl   EfficientNetB0 100   736761
57 2     ctl   EfficientNetB0 100   736733
58 2     ctl   EfficientNetB0 100   736744
59 1     ctl   EfficientNetB0 100   736758
60 1     ctl   EfficientNetB0 100   736759
61 2     ctl   EfficientNetB0 100   736756
62 2     ctl   EfficientNetB0 100   736765
#+end_example

** Remove runs with error
#+begin_src R :results output :session *R* :exports both
df.clean <- df |> filter(!is.na(epoca)) |>
  group_by(nodes, type, model, batch, id) |>
  filter(n() == 5) |>
  ungroup()


df.clean
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 680 × 7
   id     model          batch type  nodes epoca tempo
   <chr>  <chr>          <chr> <chr> <chr> <chr> <chr>
 1 735858 EfficientNetB0 32    ctl   1     0     333.319829463959
 2 735858 EfficientNetB0 32    ctl   1     1     312.819135427475
 3 735858 EfficientNetB0 32    ctl   1     2     319.11979341507
 4 735858 EfficientNetB0 32    ctl   1     3     319.29111123085
 5 735858 EfficientNetB0 32    ctl   1     4     314.691659450531
 6 735895 EfficientNetB0 32    ctl   1     0     326.055331468582
 7 735895 EfficientNetB0 32    ctl   1     1     312.437191724777
 8 735895 EfficientNetB0 32    ctl   1     2     312.249012231827
 9 735895 EfficientNetB0 32    ctl   1     3     313.085581064224
10 735895 EfficientNetB0 32    ctl   1     4     312.25504565239
# ℹ 670 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

** Graphs style
#+begin_src R :results output :session *R* :exports both

meu_estilo <- function() {
  list(
    theme_bw(base_size = 20),
    theme(
      legend.title = element_blank(),
      plot.margin = unit(c(0, 0, 0, 0), "cm"),
      legend.spacing = unit(1, "mm"),
      legend.position = "top",
      legend.justification = "left",
      legend.box.spacing = unit(0, "pt"),
      legend.box.margin = margin(0, 0, 0, 0),
      axis.text.x = element_text(angle=45, vjust=1, hjust=1)
    ))
}
#+end_src

#+RESULTS:

** Plot results

Filtering the data for the graphs
#+begin_src R :results output :session *R* :exports both
df.clean |> filter(!is.na(epoca)) |> #group_by(model, batch, type, nodes, epoca_) |>
  filter(grepl("*B0*", model)) |>

  mutate(
    # 'epoca' must be numeric for plotting
    epoca = as.numeric(epoca),
    # 'tempo' must be numeric for plotting
    tempo = as.numeric(tempo),
    # Create a unique identifier for each configuration/run (e.g., combining ID and Configs)
    # Replace 'config_column_1' and 'config_column_2' with your actual column names.
    config_run = paste(model, batch, type, nodes, sep = "_")
  ) |>
  group_by(id) |>
  mutate(tempo_treino = sum(tempo)) |>
  ungroup() -> data_plot

data_plot
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 310 × 9
   id     model          batch type  nodes epoca tempo config_run              tempo_treino
   <chr>  <chr>          <chr> <chr> <chr> <dbl> <dbl> <chr>                          <dbl>
 1 735858 EfficientNetB0 32    ctl   1         0  333. EfficientNetB0_32_ctl_1        1599.
 2 735858 EfficientNetB0 32    ctl   1         1  313. EfficientNetB0_32_ctl_1        1599.
 3 735858 EfficientNetB0 32    ctl   1         2  319. EfficientNetB0_32_ctl_1        1599.
 4 735858 EfficientNetB0 32    ctl   1         3  319. EfficientNetB0_32_ctl_1        1599.
 5 735858 EfficientNetB0 32    ctl   1         4  315. EfficientNetB0_32_ctl_1        1599.
 6 735895 EfficientNetB0 32    ctl   1         0  326. EfficientNetB0_32_ctl_1        1576.
 7 735895 EfficientNetB0 32    ctl   1         1  312. EfficientNetB0_32_ctl_1        1576.
 8 735895 EfficientNetB0 32    ctl   1         2  312. EfficientNetB0_32_ctl_1        1576.
 9 735895 EfficientNetB0 32    ctl   1         3  313. EfficientNetB0_32_ctl_1        1576.
10 735895 EfficientNetB0 32    ctl   1         4  312. EfficientNetB0_32_ctl_1        1576.
# ℹ 300 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

*** See how the time was by epoch

#+begin_src R :results output file graphics :file (org-babel-temp-file "figure" ".png") :exports both :width 1200 :height 800 :session *R*

data_plot |>
  group_by(config_run, epoca) |>
  mutate(
    N = n(),
    mean_tempo = mean(tempo, na.rm = TRUE),
    sd_tempo = sd(tempo),
    se_tempo = 3 * sd(tempo) / sqrt(N),
  ) |>
  mutate(
    lower_bound = mean_tempo - se_tempo,
    upper_bound = mean_tempo + se_tempo
  ) |>
  ggplot(aes(x = epoca, y = mean_tempo, color = as.factor(epoca))) +
  geom_jitter(
    data = data_plot,
    aes(x = epoca, y = tempo, color = as.factor(epoca), group = config_run),
    # Set the transparency (alpha) to 0.3 for a faint look
    alpha = 0.3,
    size = 2
  ) +
  geom_line(linewidth = 1, alpha = 0) +
  # Add points for the mean of the epochs
  geom_point(size = 3, alpha = 0) +
  geom_errorbar(
    aes(ymin = lower_bound, ymax = upper_bound),
    width = 0.2, # Controls the width of the horizontal bar at the end of the error line
    linewidth = 0.6, alpha = 0
  ) +
  facet_grid(batch~type ~ nodes) +
  meu_estilo()
#+end_src

#+RESULTS:
[[file:/tmp/babel-syZuVj/figurerlSYNq.png]]

*** See time by training

#+begin_src R :results output graphics file :file (org-babel-temp-file "figure" ".png") :exports both :width 600 :height 400 :session *R*

data_plot |>
  group_by(id, type, model, batch, nodes) |>
  summarize(
    tempo_treino = sum(tempo, na.rm = TRUE),
    ) |>
  ungroup() |>
  mutate(batch = as.integer(batch)) |>
  print() |>
  ##   mutate(
  ##   lower_bound = mean_tempo - se_tempo,
  ##   upper_bound = mean_tempo + se_tempo
  ## ) |>

  ggplot(aes(x = as.factor(nodes), y = tempo_treino, color = as.factor(nodes))) +

  geom_jitter(    # Set the transparency (alpha) to 0.3 for a faint look
    alpha = 0.8,
    size = 2
  ) +
  geom_line(linewidth = 1, alpha = 0) +

    facet_grid(type ~ batch) +
  labs(
    y = "Training Time [Seconds]",
    x = "Nodes"
  ) +

meu_estilo()
#+end_src

#+RESULTS:
[[file:/tmp/babel-syZuVj/figureDHaYfk.png]]

*** Resultados por batch
#+begin_src R :results output file graphics  :file (org-babel-temp-file "figure" ".pdf") :exports results :width 8 :height 4 :session *R*

data_plot |> #filter(grepl("*50*", model)) |>
  group_by(id, type, model, batch) |>
  mutate(
    tempo_treino = sum(tempo, na.rm = TRUE) / 60,
  ) |>
  ungroup() |>
  mutate(batch = as.integer(batch)) -> data_treino

data_treino |> group_by(id, type, model, batch) |>
  summarize(
    tempo_treino = sum(tempo)/60
  ) |> ungroup() -> data_points


data_treino |>
  group_by(type, model, batch) |>
  summarize(
    N = n(),
    mean_treino = mean(tempo_treino, na.rm = TRUE),
    sd_treino = sd(tempo_treino),
    se_treino = 3 * sd(tempo_treino) / sqrt(N),
  ) |>
  ungroup() |>
  mutate(
    lower_bound = mean_treino - se_treino,
    upper_bound = mean_treino + se_treino
  ) -> data_treino.plot

data_treino.plot |>
  ggplot(aes(x = as.factor(batch), y = mean_treino,
             color=model,
             fill=model)) +


  ## geom_line(aes(y = mean_treino, group = model), # <--- Mapeie a linha para a média
  ##         linewidth = 0.3
  ##       , alpha = 1) +

  geom_col(  # Set the transparency (alpha) to 0.3 for a faint look
    position = position_dodge(width = 0.9)
  ) +


  geom_jitter(
    data = data_points,
    aes(x = as.factor(batch), y = tempo_treino), # Mapeamento X/Y do dado bruto
    # Adicione color=model NO AES() para que o dodge funcione
    # Você já tem fill=model e color=model no mapeamento global.
    # Se você quiser uma cor neutra para os pontos:
    color = "gray30", # Cor fixa fora do aes()
    alpha = 0.3,
    size = 1.5,
    # Use a mesma posição de desvio EXATA
    position = position_jitterdodge(dodge.width = 0.9,
        jitter.width = 0.35)
  ) +
  ## geom_line(linewidth = 1, alpha = 0) +


  geom_errorbar(aes( ymin = lower_bound, ymax = upper_bound),
                width=.8, alpha=.7,
                position = position_dodge(width = 0.9),
                color="black") +

  facet_grid( ~ type) +
  labs(
    y = "Training Time [Minutes]",
    x = "Nodes"
  ) +
  scale_fill_manual(values = c("red", "blue")) +
  scale_color_manual(values = c("red", "blue")) +

  meu_estilo()
#+end_src

#+RESULTS:
[[file:/tmp/babel-IGtNc7/figureAM27lO.pdf]]


*** Resultados por número de nodos
#+begin_src R :results output file graphics  :file (org-babel-temp-file "figure" ".pdf") :exports results :width 8 :height 4 :session *R*

data_plot |> #filter(grepl("*50*", model)) |>
  group_by(id, type, model, nodes) |>
  mutate(
    tempo_treino = sum(tempo, na.rm = TRUE) / 60,
  ) |>
  ungroup() |>
  mutate(batch = as.integer(batch)) -> data_treino

data_treino |> group_by(id, type, model, nodes) |>
  summarize(
    tempo_treino = sum(tempo)/60
  ) |> ungroup() -> data_points


data_treino |>
  group_by(type, model, nodes) |>
  summarize(
    N = n(),
    mean_treino = mean(tempo_treino, na.rm = TRUE),
    sd_treino = sd(tempo_treino),
    se_treino = 3 * sd(tempo_treino) / sqrt(N),
  ) |>
  ungroup() |>
  mutate(
    lower_bound = mean_treino - se_treino,
    upper_bound = mean_treino + se_treino
  ) -> data_treino.plot

data_treino.plot |>
  ggplot(aes(x = as.factor(nodes), y = mean_treino,
             color=model,
             fill=model)) +


  ## geom_line(aes(y = mean_treino, group = model), # <--- Mapeie a linha para a média
  ##         linewidth = 0.3
  ##       , alpha = 1) +

  geom_col(  # Set the transparency (alpha) to 0.3 for a faint look
    position = position_dodge(width = 0.9)
  ) +


  geom_jitter(
    data = data_points,
    aes(x = as.factor(nodes), y = tempo_treino), # Mapeamento X/Y do dado bruto
    # Adicione color=model NO AES() para que o dodge funcione
    # Você já tem fill=model e color=model no mapeamento global.
    # Se você quiser uma cor neutra para os pontos:
    color = "gray30", # Cor fixa fora do aes()
    alpha = 0.3,
    size = 1.5,
    # Use a mesma posição de desvio EXATA
    position = position_jitterdodge(dodge.width = 0.9,
        jitter.width = 0.35)
  ) +
  ## geom_line(linewidth = 1, alpha = 0) +


  geom_errorbar(aes( ymin = lower_bound, ymax = upper_bound),
                width=.8, alpha=.7,
                position = position_dodge(width = 0.9),
                color="black") +

  facet_grid( ~ type) +
  labs(
    y = "Training Time [Minutes]",
    x = "Nodes"
  ) +
  scale_fill_manual(values = c("red", "blue")) +
  scale_color_manual(values = c("red", "blue")) +

  meu_estilo()
#+end_src



*** Aglomerado final dos dados
#+begin_src R :results output file graphics  :file (org-babel-temp-file "figure" ".pdf") :exports results :width 8 :height 4 :session *R*

data_plot |> #filter(grepl("*B0*", model)) |>
  group_by(id, type, model, batch, nodes) |>
  mutate(
    tempo_treino = sum(tempo, na.rm = TRUE) / 60,
  ) |>
  ungroup() |>
  mutate(batch = as.integer(batch)) -> data_treino

data_treino |> group_by(id, type, model, batch, nodes) |>
  summarize(
    tempo_treino = sum(tempo)/60
  ) |> ungroup() -> data_points


data_treino |>
  group_by(type, model, batch, nodes) |>
  summarize(
    N = n(),
    mean_treino = mean(tempo_treino, na.rm = TRUE),
    sd_treino = sd(tempo_treino),
    se_treino = 3 * sd(tempo_treino) / sqrt(N),
  ) |>
  ungroup() |>
  mutate(
    lower_bound = mean_treino - se_treino,
    upper_bound = mean_treino + se_treino
  ) -> data_treino.plot

data_treino.plot |>
  ggplot(aes(x = as.factor(nodes), y = mean_treino,
             color=model,
             fill=model)) +


  ## geom_line(aes(y = mean_treino, group = model), # <--- Mapeie a linha para a média
  ##         linewidth = 0.3
  ##       , alpha = 1) +

  geom_col(  # Set the transparency (alpha) to 0.3 for a faint look
    position = position_dodge(width = 0.9)
  ) +


  geom_jitter(
    data = data_points,
    aes(x = as.factor(nodes), y = tempo_treino), # Mapeamento X/Y do dado bruto
    # Adicione color=model NO AES() para que o dodge funcione
    # Você já tem fill=model e color=model no mapeamento global.
    # Se você quiser uma cor neutra para os pontos:
    color = "gray30", # Cor fixa fora do aes()
    alpha = 0.3,
    size = 1.5,
    # Use a mesma posição de desvio EXATA
    position = position_jitterdodge(dodge.width = 0.9,
        jitter.width = 0.35)
  ) +
  ## geom_line(linewidth = 1, alpha = 0) +


  geom_errorbar(aes( ymin = lower_bound, ymax = upper_bound),
                width=.8, alpha=.7,
                position = position_dodge(width = 0.9),
                color="black") +

  facet_grid(batch ~ type) +
  labs(
    y = "Training Time [Minutes]",
    x = "Nodes"
  ) +
  scale_fill_manual(values = c("red", "blue")) +
  scale_color_manual(values = c("red", "blue")) +

  meu_estilo()
#+end_src

#+RESULTS:
: `summarise()` has grouped output by 'id', 'type', 'model', 'batch'. You can override using the `.groups`
: argument.
: `summarise()` has grouped output by 'type', 'model', 'batch'. You can override using the `.groups`
: argument.


** Creating a model

*** Create a separated dataset for each Model-Type combination

Function to process data for all columns
#+begin_src R :results output :session *R* :exports both
get_data_comb <- function(df, mod, typ){

  ret <- df |> filter(grepl(mod, model)) |> filter(type == typ) |> group_by(id) |>
    mutate(tempo_treino = sum(as.numeric(tempo))) |>
    mutate(nodes = as.numeric(nodes)) |>
    mutate(batch = as.numeric(batch)) |>
    select(-epoca, -tempo) |>
    ungroup()


  return(ret)
}
#+end_src

#+RESULTS:

#+begin_src R :results output :session *R* :exports both

df.eff.ctl <- get_data_comb(df.clean, "B0", "ctl")

df.eff.sync <- get_data_comb(df.clean, "B0", "sync")

df.res.ctl <- get_data_comb(df.clean, "50", "ctl")

df.res.sync <- get_data_comb(df.clean, "50", "sync")

df.eff.ctl |> select(nodes, batch, tempo_treino) |> arrange(nodes)
df.res.sync |> filter(nodes == 2)
#+end_src

#+RESULTS:
#+begin_example
# A tibble: 169 × 3
   nodes batch tempo_treino
   <dbl> <dbl>        <dbl>
 1     1   100         350.
 2     1   100         678.
 3     1   100         678.
 4     1   100         345.
 5     1   100         345.
 6     1   100         680.
 7     1   100         680.
 8     1   100         677.
 9     1   100         677.
10     1   100         350.
# ℹ 159 more rows
# ℹ Use `print(n = ...)` to see more rows
# A tibble: 75 × 6
   id     model    batch type  nodes tempo_treino
   <chr>  <chr>    <dbl> <chr> <dbl>        <dbl>
 1 735840 ResNet50   100 sync      2        3550.
 2 735840 ResNet50   100 sync      2        3550.
 3 735840 ResNet50   100 sync      2        3550.
 4 735840 ResNet50   100 sync      2        3550.
 5 735840 ResNet50   100 sync      2        3550.
 6 735881 ResNet50   100 sync      2        3541.
 7 735881 ResNet50   100 sync      2        3541.
 8 735881 ResNet50   100 sync      2        3541.
 9 735881 ResNet50   100 sync      2        3541.
10 735881 ResNet50   100 sync      2        3541.
# ℹ 65 more rows
# ℹ Use `print(n = ...)` to see more rows
#+end_example

*** Create the models

Just change the dataset to see the other models

**** Creating the model
#+begin_src R :results output :session *R* :exports both
fit <- lm(data=df.res.sync, tempo_treino ~ nodes + batch + nodes:batch + I(nodes^2) - I(batch^2))

summary(fit)
#+end_src

#+RESULTS:
#+begin_example

Call:
lm(formula = tempo_treino ~ nodes + batch + nodes:batch + I(nodes^2) -
    I(batch^2), data = df.res.sync)

Residuals:
    Min      1Q  Median      3Q     Max
-1032.4  -432.6  -406.4   445.0  1498.7

Coefficients:
             Estimate Std. Error t value Pr(>|t|)
(Intercept) -9882.428    566.259 -17.452  < 2e-16 ***
nodes       14996.674    533.954  28.086  < 2e-16 ***
batch          31.960      5.600   5.707 4.66e-08 ***
I(nodes^2)  -2605.309    126.008 -20.676  < 2e-16 ***
nodes:batch   -44.604      2.887 -15.450  < 2e-16 ***
---
Signif. codes:  0 ‘***’ 0.001 ‘**’ 0.01 ‘*’ 0.05 ‘.’ 0.1 ‘ ’ 1

Residual standard error: 814.2 on 180 degrees of freedom
Multiple R-squared:  0.9129,	Adjusted R-squared:  0.911
F-statistic: 471.7 on 4 and 180 DF,  p-value: < 2.2e-16
#+end_example


Testing the model
#+begin_src R :results output :session *R* :exports both
predict(fit, data.frame(nodes=2, batch=100))
#+end_src

#+RESULTS:
:        1
: 4238.002

**** Plotting the model

#+begin_src R :results output file graphics  :file (org-babel-temp-file "figure" ".png") :exports both :width 600 :height 400 :session *R*
# Certifique-se de que os pacotes necessários estão carregados
library(dplyr)
library(ggplot2)

# --- 1. Reestruturar Variáveis e Modelo --- O modelo categórico é o
# ajuste mais adequado

# --- 2. Preparar Dados Agr1.96 *egados --- dados_agregados <-
dados_agregados <- df.res.sync |> group_by(nodes, batch) |> summarise(
media_tempo_observado = mean(tempo_treino), se_tempo =
sd(tempo_treino) / (3 * sqrt(n())), .groups = 'drop' )

dados_agregados$previsao_modelo <- predict(fit, newdata =
dados_agregados)

grafico_combinado <- dados_agregados |> ggplot(aes(x = batch, y=media_tempo_observado, group =
  nodes, color = as.factor(nodes))) +

  geom_errorbar(aes(ymin = media_tempo_observado - se_tempo, ymax =
                    media_tempo_observado + se_tempo), width = 3) +

  geom_point(aes(y = media_tempo_observado), size = 4, alpha = 0.4) +


geom_point(data=df.res.ctl, aes(x = batch, y = tempo_treino, colour=as.factor(nodes)),
           alpha = 0.3, # Transparência para ver sobreposição
           size = 1.5) +

  geom_line(aes(y = previsao_modelo), linewidth = 1.2, linetype =
            "solid") +

  labs( title = "Efeito de Interação (Modelo Categórico)", subtitle =
    "Linhas e Pontos representam as Médias Observadas e Previstas", x
    = "Tamanho do Batch", y = "Tempo de Treino Médio (segundos)",
    color = "Nodos" ) + meu_estilo()

# Exibir o gráfico print(grafico_combinado)
grafico_combinado
#+end_src

#+RESULTS:
[[file:/tmp/babel-AjemSq/figurezM2Fhc.png]]

#+begin_src R :results output :session *R* :exports both

# Certifique-se de que o pacote plotly está carregado
# install.packages("plotly")
library(plotly)

# Assumindo que o modelo quadrático foi ajustado:
# modelo_quadratico <- lm(tempo_treino ~ nodes + batch + I(nodes^2) + I(batch^2) + nodes:batch, data = R)

# --- A. Criar a Grade de Previsão (A Superfície Curva) ---
R <- df.res.sync
# 1. Definir os intervalos para os eixos (X e Y)
# Usaremos um número maior de pontos (e.g., 20) para obter uma superfície mais suave.
x_nodes <- seq(min(R$nodes), max(R$nodes), length.out = 20)
y_batch <- seq(min(R$batch), max(R$batch), length.out = 20)

# 2. Criar a grade de combinações
grade_previsao <- expand.grid(nodes = x_nodes, batch = y_batch)

# 3. Calcular as previsões do modelo quadrático para a grade
grade_previsao$tempo_previsto <- predict(fit, newdata = grade_previsao)

# 4. Transformar a previsão em uma matriz (Z)
z_superficie <- matrix(grade_previsao$tempo_previsto,
                       nrow = length(x_nodes),
                       ncol = length(y_batch))

# --- B. Criar o Gráfico 3D ---

plot_quadratico_3d <- plot_ly() %>%

  # Adiciona os Dados Observados (Pontos)
  add_trace(data = R, x = ~nodes, y = ~batch, z = ~tempo_treino,
            type = 'scatter3d',
            mode = 'markers',
            name = 'Dados Observados',
            marker = list(size = 5, opacity = 0.8)) %>%

  # Adiciona a Superfície Quadrática de Regressão
  add_trace(x = x_nodes, y = y_batch, z = z_superficie,
            type = 'surface',
            opacity = 0.6, # Torna a superfície semi-transparente
            name = 'Modelo Quadrático') %>%

  # Configura o Layout
  layout(title = "Superfície de Regressão Quadrática 3D",
         scene = list(
           xaxis = list(title = "Nodos"),
           yaxis = list(title = "Tamanho do Batch"),
           zaxis = list(title = "Tempo de Treino Previsto")
         ))

# Exibir o gráfico
print(plot_quadratico_3d)
#+end_src

#+RESULTS:
#+begin_example

Attaching package: ‘plotly’

The following object is masked from ‘package:ggplot2’:

    last_plot

The following object is masked from ‘package:stats’:

    filter

The following object is masked from ‘package:graphics’:

    layout
Error: unexpected '<' in "R -<"
Error in seq(min(R$nodes), max(R$nodes), length.out = 20) :
  object 'R' not found
Error in seq(min(R$batch), max(R$batch), length.out = 20) :
  object 'R' not found
Error in expand.grid(nodes = x_nodes, batch = y_batch) :
  object 'x_nodes' not found
Error in predict.lm(fit, newdata = grade_previsao) :
  object 'grade_previsao' not found
Error in matrix(grade_previsao$tempo_previsto, nrow = length(x_nodes),  :
  object 'grade_previsao' not found
Error in add_trace(., x = x_nodes, y = y_batch, z = z_superficie, type = "surface",  :
  object 'x_nodes' not found
Error in print(plot_quadratico_3d) :
  object 'plot_quadratico_3d' not found
#+end_example
